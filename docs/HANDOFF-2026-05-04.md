# Handoff — 4 de Maio de 2026

**Versão:** 1.6.0  
**Branch:** main (2 commits à frente de origin/main)  
**Estado:** Pronto para push

---

## Estado Actual do Projecto

### Versão e Ambiente
- **Versão actual:** 1.6.0 (ver `docs/CHANGELOG.md`)
- **Stack:** Next.js 15 (App Router) · Supabase · Stripe · Asaas · Resend · Vercel
- **Supabase Staging:** `wrqjpyyopwgyqluqkcga`
- **Supabase Produção:** `brjumbfpvijrkhrherpt`
- **Domínio produção:** `www.homestay.pt`

---

## O que foi implementado (v1.5.0 → v1.6.0)

### Epic 16 — Dados de Hóspedes nas Reservas ✅
- Colunas `adults`, `children`, `notes` adicionadas à tabela `reservations`
- Formulários de criação e edição de reserva com os novos campos
- Página de detalhe e impressão/PDF mostram os dados
- PDF de relatório de reservas inclui colunas Adultos, Crianças, Notas

### Epic 17 — Anexos de Documentos ✅
- **Despesas:** upload de PDF/JPEG/Word/Excel (≤20MB, até 5 ficheiros); download via signed URL; eliminação
- **Propriedades:** upload de documentos (≤20MB) e vídeos mp4/mov (≤100MB); campo "URLs das Fotos" removido do formulário
- Componente `FileUpload` reutilizável com drag-and-drop
- Rotas API completas com RLS e signed URLs

### Filtro por Mês com Navegação ✅
- Componente `MonthNavigator` partilhado (URL-based: `?month=YYYY-MM`)
- `/reservations` e `/financial` filtram por mês com navegação ← →

### Multicurrency ✅
- Soma cross-currency corrigida: relatório de proprietário agrupa por moeda
- Tabela financeira mostra badge de moeda por propriedade

### Segurança — `getUser()` ✅
- Todas as chamadas `getSession()` substituídas por `getUser()` em código servidor
- Previne tokens revogados a passar validação de autenticação

### Routing — RSC 404 ✅
- Middleware usa `rewrite()` para pedidos RSC em vez de `redirect()`
- 16 páginas `[locale]` com hrefs locale-aware

---

## O que está pendente (não implementado)

### Epic 18 — Property Amenities
- Stories 18.1–18.2 (CRUD de amenities + associação a propriedades)
- Dependência de epics 16–17 concluída; pode começar

### i18n — Expansão Global
- Stories `i18n.1`–`i18n.4` (internacionalização da app interna)
- Página `/booking` mantida em PT até validação de mercado ES/UK

### Google Vacation Rentals Distribution
- Epic descrito em `docs/stories/epic-google-distribution-roadmap.md`
- Bloqueado por: structured data Schema.org, imagens otimizadas, disponibilidade em tempo real

### Outros Itens Identificados
- Remover bypass `admin@dev.com` de `useAuth.ts` (criado em emergência no sprint de Abril)
- E2E CI/CD — GitHub Secrets ainda por configurar (`docs/guides/E2E_CI_CD_SETUP.md`)
- Evidência fotográfica no checklist de limpeza (campo visual, upload real pendente)

---

## Para Continuar

### Push para Produção
```bash
# Os 2 commits locais (pós-origin/main) precisam de push via @devops:
git push origin main
```

### Próxima História Sugerida
`docs/stories/epic-18-property-amenities.md` → Stories 18.1 e 18.2

### Testes
```bash
npm run lint        # deve passar sem erros
npm run typecheck   # deve passar sem erros
npm test            # suite completa
```

---

## Ficheiros Críticos de Referência

| Ficheiro | Propósito |
|----------|-----------|
| `docs/CHANGELOG.md` | Histórico completo de versões |
| `docs/stories/` | Stories e epics de desenvolvimento |
| `supabase/migrations/` | Histórico de migrações DB |
| `src/lib/auth/getUserAccess.ts` | Auth server-side centralizado |
| `middleware.ts` | Auth guard + locale routing + RSC rewrite |
| `src/components/common/ui/FileUpload.tsx` | Componente de upload reutilizável |
| `src/components/common/ui/MonthNavigator.tsx` | Navegação por mês |
