# Story 16.1: Migração DB — colunas adults e children em reservations

**Epic:** 16 — Reservas: Dados de Hóspedes e PDF
**Status:** Ready for Review
**Prioridade:** P1 — Fundação: stories 16.2, 16.3, 16.4 dependem disto
**Tipo:** Backend / Database
**Agente:** @dev

---

## Story

**Como** gestor de propriedades,
**Quero** registar o número de adultos e crianças em cada reserva,
**Para que** tenha dados de ocupação correctos para operações e futura distribuição.

---

## Acceptance Criteria

- [x] **AC1** — Coluna `adults` (integer, NOT NULL, DEFAULT 1) adicionada à tabela `reservations`
- [x] **AC2** — Coluna `children` (integer, NOT NULL, DEFAULT 0) adicionada à tabela `reservations`
- [x] **AC3** — Migração Supabase criada e aplicada
- [x] **AC4** — RLS policies não afectadas
- [x] **AC5** — Tipo TypeScript actualizado para incluir os novos campos

---

## Dev Notes

### Migração SQL

```sql
ALTER TABLE reservations
  ADD COLUMN adults integer NOT NULL DEFAULT 1,
  ADD COLUMN children integer NOT NULL DEFAULT 0;
```

### Ficheiros a actualizar

- `supabase/migrations/` — nova migração
- `src/types/` ou equivalente — adicionar `adults` e `children` ao tipo `Reservation`

---

## Tasks

- [x] Criar ficheiro de migração Supabase
- [x] Aplicar migração no ambiente de staging
- [x] Actualizar tipos TypeScript
- [x] Verificar que queries existentes não quebram

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- Migração com IF NOT EXISTS para idempotência
- Check constraints adicionados: adults >= 1, children >= 0
- Tipo `notes` também adicionado ao `Reservation` (estava em falta)
- `ReservationUI` actualizado com campos opcionais (nullable) para compatibilidade retroactiva

### File List
- `supabase/migrations/20260503_01_reservation_guest_fields.sql` (new)
- `src/types/database.ts` (modified)
- `src/components/features/reservations/types/reservation-ui.ts` (modified)

### Change Log
- 2026-05-03: Story 16.1 completa (Dex, claude-sonnet-4-6)
