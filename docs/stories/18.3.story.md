# Story 18.3: Quartos — tipos de cama, capacidade e lençóis

**Epic:** 18 — Propriedades: Comodidades, Quartos, Banheiros e Taxas
**Status:** Ready for Review
**Prioridade:** P2 — Depende de 18.1
**Tipo:** Full Stack
**Agente:** @dev

---

## Story

**Como** gestor de propriedades,
**Quero** configurar os quartos da propriedade com tipo de cama, capacidade e disponibilidade de lençóis,
**Para que** os hóspedes saibam exactamente o que encontram ao chegar.

---

## Acceptance Criteria

- [x] **AC1** — Secção "Quartos" no formulário de edição de propriedade
- [x] **AC2** — Adicionar múltiplos quartos (botão "Adicionar Quarto")
- [x] **AC3** — Cada quarto: nome (opcional), tipo de cama, nº de camas, disponibiliza lençóis (Sim/Não)
- [x] **AC4** — Tipos de cama: Solteiro, Casal, Queen, King, Sofá-cama, Beliche
- [x] **AC5** — Remoção de quarto com confirmação
- [x] **AC6** — Dados persistidos em `property_rooms`
- [x] **AC7** — Quartos listados no perfil da propriedade

---

## Dev Notes

### Tipos de cama

```typescript
const BED_TYPES = [
  { value: 'single', label: 'Solteiro' },
  { value: 'double', label: 'Casal' },
  { value: 'queen', label: 'Queen' },
  { value: 'king', label: 'King' },
  { value: 'sofa_bed', label: 'Sofá-cama' },
  { value: 'bunk', label: 'Beliche' },
]
```

### UI pattern

Lista dinâmica de quartos com add/remove. Cada item é um card expansível ou inline form.

---

## Tasks

- [x] Criar secção "Quartos" no formulário
- [x] Lista dinâmica: adicionar/remover quartos
- [x] Campos: nome, tipo de cama (select), nº camas (number), lençóis (checkbox)
- [x] CRUD de quartos via API
- [x] Listar quartos no perfil da propriedade

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- `RoomsEditor` componente client com lista dinâmica de quartos (add/remove/edit)
- Remoção com confirmação inline (`confirmingId` state — mostra Sim/Cancelar antes de remover)
- PUT usa delete-then-insert (full replace), consistente com padrão 18.2
- `sort_order` preservado pela posição no array
- Perfil da propriedade lista quartos com bed_type localizado e indicador de lençóis

### File List
- `src/components/features/properties/RoomsEditor.tsx` (new)
- `src/app/api/properties/[id]/rooms/route.ts` (new)
- `src/app/[locale]/properties/[id]/edit/page.tsx` (modified)
- `src/app/[locale]/properties/[id]/page.tsx` (modified)

### Change Log
- 2026-05-06: Story 18.3 completa — RoomsEditor + API de quartos (Dex, claude-sonnet-4-6)

---

## QA Results

### Review Date: 2026-05-06

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementação consistente com o padrão 18.2. Error handling correctamente aplicado no load(). Um problema encontrado e corrigido: save() partilhava o mesmo estado `error` que o load(), causando destruição do formulário em caso de falha ao guardar. Separados em `loadError` e `saveError`.

### Refactoring Performed

- **File:** `src/components/features/properties/RoomsEditor.tsx`
  - **Change:** Separado estado `error` em `loadError` e `saveError`
  - **Why:** `save()` chamava `setError()` que activava o guard `if (error) return`, destruindo o formulário inteiro. Erros de load devem bloquear o componente; erros de save devem ser inline e permitir retry
  - **How:** `loadError` bloqueia o render; `saveError` aparece inline junto ao botão "Guardar Quartos"

### Compliance Check

- Coding Standards: ✓ Padrão idêntico a 18.2, sem magic strings
- Project Structure: ✓ Componente em `features/properties/`, API em `app/api/`
- Testing Strategy: ✓ N/A — UI story, padrão validado em 18.2
- All ACs Met: ✓ AC1–AC7 todos implementados e verificados

### Requirements Traceability

| AC | Implementação | Verificação |
|----|--------------|-------------|
| AC1 Secção no formulário | `RoomsEditor` em `edit/page.tsx` | ✓ Após secção Comodidades |
| AC2 Adicionar Quarto | `addRoom()` + botão com ícone Plus | ✓ Adiciona card inline |
| AC3 Nome, tipo cama, nº camas, lençóis | 4 campos por quarto | ✓ Nome opcional, defaults correctos |
| AC4 6 tipos de cama | `BED_TYPE_LABELS` completo | ✓ single/double/queen/king/sofa_bed/bunk |
| AC5 Remoção com confirmação | `confirmingId` state → Sim/Cancelar | ✓ Não remove sem confirmação |
| AC6 Persistência | PUT delete-then-insert em `property_rooms` | ✓ `sort_order` por posição no array |
| AC7 Quartos no perfil | `roomsData` em `page.tsx` com grid 2 colunas | ✓ Tipo localizado + indicador lençóis |

### Improvements Checklist

- [x] `save()` error separado de `load()` error — formulário mantém-se após falha ao guardar (RoomsEditor.tsx)
- [ ] `BED_TYPE_LABELS` duplicado entre `RoomsEditor.tsx` e `page.tsx` — candidato a extracção futura para `@/lib/constants/property` se mais componentes precisarem

### Security Review

✓ PASS — GET protegido por `requireRole(['admin', 'gestor', 'viewer'])`, PUT por `['admin', 'gestor']`. Input validado (`Array.isArray`). `bed_count` sanitizado via `Number() || 1`. `name` trimado e convertido para `null` se vazio.

### Performance Considerations

✓ PASS — Padrão idêntico ao 18.2. Full replace é adequado para listas pequenas (quartos por propriedade tipicamente < 20).

### Files Modified During Review

- `src/components/features/properties/RoomsEditor.tsx` — separação loadError/saveError

### Gate Status

Gate: PASS (100/100) → docs/qa/gates/18.3-rooms-editor.yml

### Recommended Status

[✓ Ready for Done]
