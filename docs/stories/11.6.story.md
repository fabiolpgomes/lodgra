# Story 11.6: Fix Multi-Tenant Isolation & RLS — Compliance DB Layer

**Epic:** 11 — Compliance (RGPD/LGPD/GDPR)
**Status:** Ready for Dev
**Prioridade:** P0 — CRITICAL (Security/Compliance)
**Tipo:** Bug Fix / Security
**Agente:** @dev
**Depende de:** 11.1, 11.4
**Origem:** Revisão arquitectural 2026-04-07 (issues C1, H1, H7)

---

## Story

**Como** administrador da plataforma,
**Quero** que os dados de consentimento e eliminação sejam isolados por organização,
**Para que** um admin de Org A nunca veja dados de Org B — conforme RGPD Art. 5 (data minimization).

---

## Contexto

A revisão arquitectural identificou que `consent_records` e `deletion_requests` **não têm `organization_id`**. As RLS policies de admin fazem check de role mas não de organização, permitindo cross-tenant data visibility. Adicionalmente:
- A policy UPDATE de `deletion_requests` não tem `WITH CHECK` — user pode alterar `status` directamente
- FK cascading inconsistente: `consent_records` usa SET NULL, `deletion_requests` usa CASCADE (perde audit trail)

---

## Critérios de Aceitação

- [ ] **AC1** — Coluna `organization_id` (NOT NULL, FK) adicionada a `consent_records` com index
- [ ] **AC2** — Coluna `organization_id` (NOT NULL, FK) adicionada a `deletion_requests` com index
- [ ] **AC3** — RLS policies de admin filtram por `organization_id` do utilizador autenticado (ambas tabelas)
- [ ] **AC4** — RLS UPDATE de `deletion_requests` tem WITH CHECK: apenas permite `status = 'cancelled'` pelo user
- [ ] **AC5** — FK cascading uniformizado: ambas tabelas usam `ON DELETE SET NULL` (preserva audit trail)
- [ ] **AC6** — Constraint `scheduled_at > requested_at` em `deletion_requests`
- [ ] **AC7** — API routes `/api/consent`, `/api/user/delete-request`, `/api/user/cancel-deletion` passam `organization_id` no insert
- [ ] **AC8** — Admin compliance API filtra por org do admin autenticado
- [ ] **AC9** — Testes unitários verificam isolamento cross-org
- [ ] **AC10** — Migration reversível (DOWN function)

---

## Tasks

- [ ] 1. Criar migração `20260407_02_fix_compliance_org_isolation.sql`:
  - ADD COLUMN `organization_id` a `consent_records` (default from user_profiles join for backfill)
  - ADD COLUMN `organization_id` a `deletion_requests` (default from user_profiles join for backfill)
  - CREATE INDEX em ambas colunas
  - ADD CONSTRAINT `scheduled_at > requested_at`
- [ ] 2. Reescrever RLS policies:
  - `consent_records`: admin SELECT filtra por org_id do auth.uid()
  - `consent_records`: INSERT inclui org_id check
  - `deletion_requests`: admin SELECT filtra por org_id
  - `deletion_requests`: UPDATE com WITH CHECK (status = 'cancelled' AND user_id = auth.uid())
- [ ] 3. Uniformizar FK cascading — ALTER `deletion_requests.user_id` para `ON DELETE SET NULL`
- [ ] 4. Actualizar `POST /api/consent` — incluir `organization_id` no insert (de `requireRole()` ou session)
- [ ] 5. Actualizar `POST /api/user/delete-request` — incluir `organization_id`
- [ ] 6. Actualizar `GET /api/admin/compliance` — filtrar por org do admin
- [ ] 7. Actualizar `GET /api/admin/compliance/csv` — filtrar por org do admin
- [ ] 8. Testes: verificar que admin Org A não vê consent records de Org B
- [ ] 9. Run `npm run build && npm run lint && npm test`

---

## Dev Notes

### Backfill strategy

```sql
-- Backfill organization_id from user_profiles
UPDATE consent_records cr
SET organization_id = (
  SELECT organization_id FROM user_profiles up WHERE up.id = cr.user_id
)
WHERE cr.user_id IS NOT NULL AND cr.organization_id IS NULL;

-- For anonymous records (user_id IS NULL), set default org
UPDATE consent_records
SET organization_id = '00000000-0000-0000-0000-000000000001'
WHERE user_id IS NULL AND organization_id IS NULL;
```

### RLS pattern para admin org-scoped

```sql
CREATE POLICY "consent_records_select_admin_org"
  ON consent_records FOR SELECT
  USING (
    organization_id = (SELECT organization_id FROM user_profiles WHERE id = auth.uid())
    AND EXISTS (SELECT 1 FROM user_profiles WHERE id = auth.uid() AND role = 'admin')
  );
```

### UPDATE WITH CHECK pattern

```sql
CREATE POLICY "deletion_requests_update_own"
  ON deletion_requests FOR UPDATE
  USING (user_id = auth.uid() AND status = 'pending')
  WITH CHECK (status = 'cancelled' AND user_id = auth.uid());
```

---

## File List

_(a preencher pelo @dev)_

---

## Dev Agent Record

### Agent Model Used
_(a preencher)_

### Completion Notes
_(a preencher)_

### Change Log
_(a preencher)_
