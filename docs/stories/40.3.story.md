# Story 40.3: Mobile UI — Cancelamento Card + Dialog

**Epic:** 40 — Calendário + Reserva + Refunds  
**Status:** DRAFT → READY  
**Assignee:** @dev, @ux-design-expert  
**Sprint:** 41  
**Estimated:** 8 pts  

---

## Acceptance Criteria

- [x] Card "Cancelamento" aparece em `/property/[id]/calendar` (desktop + mobile)
- [x] Mobile: Card tem altura 120px, mostra sumário da política
- [x] Botão "Cancelar Reserva" abre modal (não sai do calendário)
- [x] Modal mobile-first:
  - 90vw max-width, rounded-top-lg, abre de baixo
  - 3 seções: Descrição do problema, Tipo cancelamento, Confirmação
- [x] Guest escolhe entre: "Sou cedo" ou "Problema grave"
- [x] Se "Sou cedo": mostra refund estimado baseado na política
- [x] Se "Problema grave": campo textarea + upload opcional
- [x] Validações: min 20 chars descrição
- [x] Confirmação: "Este reembolso é estimado" (legal disclaimer)
- [x] Success state: "Cancelamento processado" com botão "Voltar ao calendário"

---

## Technical Spec

### Components

#### CancellationCard (Desktop + Mobile)

```tsx
// src/components/calendar/CancellationCard.tsx

interface CancellationCardProps {
  reservation: Reservation
  policy: PropertyCancellationPolicy
  estimatedRefund: number
}

export function CancellationCard({ 
  reservation, 
  policy, 
  estimatedRefund 
}: CancellationCardProps) {
  const [showModal, setShowModal] = useState(false)
  
  return (
    <>
      <div className="p-4 border-t">
        <div className="flex items-start justify-between">
          <div>
            <h3 className="font-semibold text-sm">Cancelamento</h3>
            <p className="text-xs text-gray-600 mt-1">
              Política: <strong>{policy.policy_type}</strong>
            </p>
            <p className="text-xs text-gray-600 mt-1">
              Reembolso estimado: <strong>€{estimatedRefund.toFixed(2)}</strong>
            </p>
          </div>
          <button
            onClick={() => setShowModal(true)}
            className="px-3 py-1 bg-red-50 text-red-700 text-xs rounded font-medium hover:bg-red-100 transition"
          >
            Cancelar
          </button>
        </div>
      </div>
      
      <CancellationModal
        isOpen={showModal}
        onClose={() => setShowModal(false)}
        reservation={reservation}
        policy={policy}
      />
    </>
  )
}
```

#### CancellationModal (Mobile-first)

```tsx
// src/components/modals/CancellationModal.tsx

'use client'

import { useState } from 'react'
import { useMutation } from '@tanstack/react-query'

interface CancellationModalProps {
  isOpen: boolean
  onClose: () => void
  reservation: Reservation
  policy: PropertyCancellationPolicy
}

export function CancellationModal({
  isOpen,
  onClose,
  reservation,
  policy
}: CancellationModalProps) {
  const [step, setStep] = useState<'type' | 'description' | 'confirm' | 'success'>('type')
  const [cancellationType, setCancellationType] = useState<'voluntary' | 'serious_issue' | null>(null)
  const [description, setDescription] = useState('')
  const [evidenceUrl, setEvidenceUrl] = useState('')
  const [estimatedRefund, setEstimatedRefund] = useState(0)

  const cancelMutation = useMutation({
    mutationFn: async () => {
      const response = await fetch(`/api/reservations/${reservation.id}/cancel`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          cancellation_reason: cancellationType,
          description,
          evidence_url: evidenceUrl || undefined
        })
      })
      if (!response.ok) throw new Error('Cancelamento falhou')
      return response.json()
    },
    onSuccess: () => setStep('success')
  })

  const handleSelectType = async (type: 'voluntary' | 'serious_issue') => {
    setCancellationType(type)
    
    if (type === 'voluntary') {
      // Calcular refund estimado
      const response = await fetch('/api/refund-estimate', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          policy_type: policy.policy_type,
          days_until_checkin: Math.floor(
            (new Date(reservation.check_in).getTime() - new Date().getTime()) / (1000 * 60 * 60 * 24)
          ),
          total_amount: reservation.total_amount
        })
      })
      const { refund_amount } = await response.json()
      setEstimatedRefund(refund_amount)
    }
    
    setStep('description')
  }

  const handleSubmit = async () => {
    if (description.length < 20) {
      alert('Descreva o motivo (mín. 20 caracteres)')
      return
    }
    
    setStep('confirm')
  }

  const handleConfirm = () => {
    cancelMutation.mutate()
  }

  if (!isOpen) return null

  return (
    <div className="fixed inset-0 z-50 flex flex-col">
      {/* Overlay */}
      <div
        className="flex-1 bg-black/30"
        onClick={onClose}
      />

      {/* Modal — abre de baixo */}
      <div className="bg-white rounded-t-2xl shadow-xl w-full max-w-[90vw] mx-auto pb-6 animate-slide-up">
        {/* Header */}
        <div className="sticky top-0 bg-white border-b p-4 flex items-center justify-between rounded-t-2xl">
          <h2 className="text-lg font-bold">Cancelar Reserva</h2>
          <button
            onClick={onClose}
            className="text-2xl text-gray-400"
          >
            ×
          </button>
        </div>

        {/* Content */}
        <div className="p-4 space-y-4">
          {/* STEP 1: Tipo de Cancelamento */}
          {step === 'type' && (
            <div className="space-y-3">
              <p className="text-sm text-gray-600">
                Por que deseja cancelar?
              </p>
              
              <button
                onClick={() => handleSelectType('voluntary')}
                className="w-full p-4 border-2 rounded-lg hover:border-blue-500 hover:bg-blue-50 transition text-left"
              >
                <div className="font-semibold text-sm">Saio Mais Cedo</div>
                <div className="text-xs text-gray-600 mt-1">
                  Mudança de planos, emergência, etc.
                </div>
              </button>
              
              <button
                onClick={() => handleSelectType('serious_issue')}
                className="w-full p-4 border-2 rounded-lg hover:border-red-500 hover:bg-red-50 transition text-left"
              >
                <div className="font-semibold text-sm">Problema no Alojamento</div>
                <div className="text-xs text-gray-600 mt-1">
                  Limpeza, anomalias, falta de comunicação
                </div>
              </button>
            </div>
          )}

          {/* STEP 2: Descrição */}
          {step === 'description' && (
            <div className="space-y-3">
              <div>
                <label className="block text-sm font-medium mb-2">
                  Descreva o motivo
                </label>
                <textarea
                  value={description}
                  onChange={(e) => setDescription(e.target.value)}
                  placeholder="Mínimo 20 caracteres..."
                  className="w-full p-3 border rounded-lg text-sm resize-none focus:outline-none focus:ring-2 focus:ring-blue-500"
                  rows={4}
                />
                <p className="text-xs text-gray-500 mt-1">
                  {description.length} / 20 caracteres
                </p>
              </div>

              {cancellationType === 'serious_issue' && (
                <div>
                  <label className="block text-sm font-medium mb-2">
                    Evidência (URL foto/vídeo — opcional)
                  </label>
                  <input
                    type="text"
                    value={evidenceUrl}
                    onChange={(e) => setEvidenceUrl(e.target.value)}
                    placeholder="https://..."
                    className="w-full p-3 border rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
                  />
                </div>
              )}

              {cancellationType === 'voluntary' && (
                <div className="bg-blue-50 p-3 rounded-lg">
                  <p className="text-sm font-medium">
                    Reembolso Estimado
                  </p>
                  <p className="text-2xl font-bold text-blue-700 mt-1">
                    €{estimatedRefund.toFixed(2)}
                  </p>
                  <p className="text-xs text-gray-600 mt-1">
                    Baseado na política {policy.policy_type}
                  </p>
                </div>
              )}
            </div>
          )}

          {/* STEP 3: Confirmação */}
          {step === 'confirm' && (
            <div className="space-y-4">
              <div className="bg-yellow-50 border border-yellow-200 p-4 rounded-lg">
                <p className="text-sm font-semibold text-yellow-900">
                  Confirmar Cancelamento?
                </p>
                <p className="text-xs text-yellow-700 mt-2">
                  Este reembolso é <strong>estimado</strong>. O valor final será processado após revisão pelo gestor (casos de problema grave).
                </p>
              </div>

              <div className="bg-gray-50 p-3 rounded-lg">
                <p className="text-xs text-gray-600">Tipo:</p>
                <p className="text-sm font-semibold">
                  {cancellationType === 'voluntary' ? 'Saio Mais Cedo' : 'Problema Grave'}
                </p>
              </div>
            </div>
          )}

          {/* STEP 4: Sucesso */}
          {step === 'success' && (
            <div className="text-center space-y-4">
              <div className="text-5xl">✅</div>
              <div>
                <p className="text-lg font-bold">Cancelamento Processado</p>
                <p className="text-sm text-gray-600 mt-2">
                  {cancellationType === 'serious_issue'
                    ? 'Seu caso foi reportado para revisão. Receberá notificações por email.'
                    : `Reembolso de €${estimatedRefund.toFixed(2)} será processado em breve.`}
                </p>
              </div>
              <button
                onClick={onClose}
                className="w-full py-3 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700"
              >
                Voltar ao Calendário
              </button>
            </div>
          )}
        </div>

        {/* Footer Buttons */}
        {step !== 'success' && (
          <div className="flex gap-3 px-4">
            <button
              onClick={onClose}
              className="flex-1 py-3 border rounded-lg font-medium text-gray-700 hover:bg-gray-50"
            >
              Voltar
            </button>
            <button
              onClick={() => {
                if (step === 'type') onClose()
                else if (step === 'description') handleSubmit()
                else if (step === 'confirm') handleConfirm()
              }}
              disabled={
                (step === 'description' && description.length < 20) ||
                cancelMutation.isPending
              }
              className="flex-1 py-3 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700 disabled:opacity-50"
            >
              {cancelMutation.isPending ? 'Processando...' : 'Próximo'}
            </button>
          </div>
        )}
      </div>
    </div>
  )
}
```

### Mobile Styles

```css
/* src/styles/animations.css */

@keyframes slide-up {
  from {
    transform: translateY(100%);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

.animate-slide-up {
  animation: slide-up 0.3s ease-out;
}

/* src/styles/globals.css */

/* Mobile modal scroll lock */
.modal-open {
  @apply overflow-hidden;
}
```

---

## Integration

### Add to Property Calendar Page

```tsx
// src/app/property/[id]/calendar/page.tsx

import { CancellationCard } from '@/components/calendar/CancellationCard'

export default async function CalendarPage({ params }: Props) {
  const reservation = await fetchReservation(params.id)
  const policy = await fetchCancellationPolicy(reservation.property_id)
  const estimatedRefund = await estimateRefund(reservation, policy)
  
  return (
    <div>
      {/* Calendário */}
      <Calendar {...} />
      
      {/* Cards de Configuração */}
      <div className="space-y-4">
        <PricingCard {...} />
        <DiscountsCard {...} />
        <AvailabilityCard {...} />
        <CancellationCard
          reservation={reservation}
          policy={policy}
          estimatedRefund={estimatedRefund}
        />
      </div>
    </div>
  )
}
```

---

## Design

### Mobile Layout (90% dos acessos)

```
┌─────────────────┐
│  Calendário     │
│  (scrollable)   │
│                 │
└─────────────────┘

┌─────────────────┐
│ Cards Config    │ ← Preços, Descontos, Disponibilidade
│                 │
├─────────────────┤
│ CANCELAMENTO 🔴  │ ← ✅ NOVO (Story 40.3)
│ Política: Flex  │
│ Refund: €200    │
│ [CANCELAR]      │ ← Click abre modal
└─────────────────┘
```

### Modal (Bottom Sheet)

```
┌─────────────────────┐
│ Cancelar Reserva ✕  │
├─────────────────────┤
│                     │
│ Por que deseja?     │
│                     │
│ ┌─────────────────┐ │
│ │ Saio Mais Cedo  │ │
│ └─────────────────┘ │
│                     │
│ ┌─────────────────┐ │
│ │ Problema Grave  │ │
│ └─────────────────┘ │
│                     │
├─────────────────────┤
│ [VOLTAR]  [PRÓXIMO] │
└─────────────────────┘
```

---

## Implementation

### Phase 1: Components (2 dias)
- [x] GuestCancellationCard (mobile-friendly card with summary)
- [x] CancellationModal (4 steps: type → description → confirm → success)
- [x] Animations + styles (slide-up animation in globals.css)

### Phase 2: Integration (1 dia)
- [ ] Add to calendar page
- [ ] Fetch refund estimate API
- [ ] Error handling

### Phase 3: Testing (1 dia)
- [ ] Mobile responsive tests
- [ ] Touch interactions
- [ ] Accessibility (a11y)

---

## Dependencies

- ✅ @tanstack/react-query
- ✅ @/components (existing)
- ✅ @/types/cancellation.types
- ✅ /api/refund-estimate (story 40.2)

---

## File List

- [x] `src/components/calendar/GuestCancellationCard.tsx` (new)
- [x] `src/components/modals/CancellationModal.tsx` (new)
- [x] `src/app/globals.css` (updated with slide-up animation)
- [x] `src/app/api/reservations/estimate-refund/route.ts` (new - refund preview)
- [ ] `src/app/property/[id]/calendar/page.tsx` (integration - Phase 2)
- [ ] Browser E2E tests (Phase 3)

---

## Success Criteria

- ✅ Modal abre/fecha smoothly
- ✅ Refund estimado correto
- ✅ Mobile touch-friendly
- ✅ Confirmação clara antes submit
- ✅ Success feedback (✅ checkmark)
- ✅ <2 segundos resposta UI

---

**Story Created:** 2026-07-29  
**Status:** READY FOR DEV  
