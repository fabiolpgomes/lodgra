# Story 31.4: Branded Email Confirmations

**Epic:** Epic 31 — Multi-Tenant Infrastructure (Branded Direct Booking)  
**Status:** In Progress  
**Priority:** High  
**Type:** Backend + Email Templates  
**Assigned to:** @dev  
**Complexity:** Medium (T-shirt: M)

---

## Story

**As** a Lodgra product owner,  
**I want** companies to receive booking confirmation emails with their own branding (logo, colors, custom message),  
**So that** customers feel they're booking with the company, not through a third-party platform.

---

## Context

Stories 31.2 and 31.3 customize the booking page visually. Story 31.4 extends that to email communications. When a customer books through `pousada.lodgra.io/booking`, they should receive a confirmation email branded as from "Pousada" with Pousada's logo and colors.

**Current State:**
- Booking confirmations sent via email service (likely SendGrid or similar)
- Emails use Lodgra branding
- No company-level customization

**Desired State:**
- Emails dynamically rendered with company branding
- Company logo in email header
- Company colors applied to email buttons/accents
- Company can customize confirmation message
- Custom from-address optional (e.g., noreply@pousada.lodgra.io)

---

## Acceptance Criteria

### AC1: Email Template Configuration Table
**Given:** An organization exists  
**When:** Admin configures email templates  
**Then:**
- [ ] `organization_email_templates` table created with fields:
  - `id` (UUID primary key)
  - `organization_id` (FK to organizations, unique)
  - `confirmation_subject` (text, default: "Booking Confirmation - [Company Name]")
  - `confirmation_message` (text, markdown, custom message shown in email body)
  - `from_email` (text, default: "noreply@lodgra.io", can be org-specific)
  - `from_name` (text, default: organization name)
  - `reply_to_email` (text, optional customer support email)
  - `include_company_logo` (boolean, default: true — uses logo from Story 31.2)
  - `footer_text` (text, optional company-specific footer)
  - `created_at` (timestamp)
  - `updated_at` (timestamp)
- [ ] RLS enforced
- [ ] Unique constraint on organization_id

### AC2: Email Template API
**Given:** Admin configures email settings  
**When:** PATCH `/api/organizations/[orgId]/email-templates`  
**Then:**
- [ ] Accepts JSON with email template fields
- [ ] Validates subject length (max 100 chars)
- [ ] Validates message length (max 5000 chars, markdown allowed)
- [ ] Validates from_email format
- [ ] Updates database
- [ ] Returns 401 if user not admin
- [ ] Returns 400 if validation fails

### AC3: Fetch Email Template for Booking Confirmation
**Given:** Booking confirmation is triggered  
**When:** GET `/api/organizations/[orgSlug]/email-templates`  
**Then:**
- [ ] Returns email template config for organization
- [ ] Includes organization branding (colors, logo from Story 31.2)
- [ ] If no custom template configured, returns defaults
- [ ] Works for internal API calls (server-side only)
- [ ] Returns 404 if organization not found

### AC4: Dynamic Email Rendering
**Given:** Customer completes booking on `pousada.lodgra.io/booking`  
**When:** Booking confirmation email is sent  
**Then:**
- [ ] Email subject uses custom subject (if configured)
- [ ] Email from-address uses custom from_email (if configured)
- [ ] Email from-name uses organization name
- [ ] Company logo appears in email header (if available)
- [ ] Company primary color used for CTA button (from Story 31.2)
- [ ] Custom confirmation message displays in email body
- [ ] Booking details (dates, property, price) included
- [ ] Company footer text displayed at bottom (if configured)
- [ ] Email renders correctly in Outlook, Gmail, Apple Mail
- [ ] Responsive design (mobile-friendly)

### AC5: Email Template Variables
**Given:** Admin writes custom confirmation message  
**When:** Email is rendered  
**Then:**
- [ ] Template supports variables: {{customerName}}, {{propertyName}}, {{checkInDate}}, {{checkOutDate}}, {{totalPrice}}, {{currency}}
- [ ] Variables replaced with actual booking data
- [ ] Missing variables replaced with empty string (no error)
- [ ] Markdown formatting preserved (bold, links, etc.)

### AC6: Admin Dashboard for Email Templates
**Given:** Admin navigates to org settings  
**When:** Admin visits `/settings/organizations/[orgId]/email-templates`  
**Then:**
- [ ] Subject field shows current subject (with char count)
- [ ] From-email input (with validation)
- [ ] From-name input
- [ ] Reply-to email input (optional)
- [ ] Rich text editor for confirmation message (markdown preview)
- [ ] Template variables reference guide ({{customerName}}, etc.)
- [ ] Checkbox: "Include company logo"
- [ ] Footer text input
- [ ] "Preview" button shows email preview
- [ ] "Send test email" button sends test to admin
- [ ] "Save" button persists to database

### AC7: Email Sent Tracking
**Given:** Email is sent  
**When:** Confirmation email is sent to customer  
**Then:**
- [ ] Event logged in database: email_sent table
  - `id`, `organization_id`, `booking_id`, `customer_email`, `sent_at`, `template_type`
- [ ] Admin can view email delivery status (bounce, click, etc.) via email service
- [ ] Unsubscribe link included in footer (GDPR compliance)

### AC8: Backward Compatibility
- [ ] Existing organizations without email templates use Lodgra defaults
- [ ] No regression in booking confirmation delivery
- [ ] All existing emails still sent (no opt-out)

---

## Dev Notes

### Technical Approach

**1. Database Migration**
```sql
CREATE TABLE organization_email_templates (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id UUID NOT NULL UNIQUE REFERENCES organizations(id) ON DELETE CASCADE,
  confirmation_subject TEXT DEFAULT 'Booking Confirmation',
  confirmation_message TEXT,
  from_email TEXT DEFAULT 'noreply@lodgra.io',
  from_name TEXT,
  reply_to_email TEXT,
  include_company_logo BOOLEAN DEFAULT true,
  footer_text TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE TABLE email_sent (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id UUID REFERENCES organizations(id),
  booking_id UUID REFERENCES bookings(id),
  customer_email TEXT NOT NULL,
  template_type TEXT DEFAULT 'confirmation',
  sent_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**2. Email Template Rendering**
- Use mjml library (MJML) for email markup
- Create template files: `src/lib/email/templates/booking-confirmation.mjml`
- Template supports Handlebars syntax for variables
- Render with company branding colors/logo

**3. Backend Logic**
```typescript
// Pseudocode
async function sendBookingConfirmation(booking, organization) {
  const template = await getEmailTemplate(organization.id);
  const branding = await getBranding(organization.id);
  
  const emailHtml = renderMJML('booking-confirmation.mjml', {
    ...booking,
    ...template,
    logo: branding.logo_url,
    primaryColor: branding.primary_color,
  });
  
  await sendEmail({
    from: template.from_email,
    to: booking.customer_email,
    subject: template.confirmation_subject,
    html: emailHtml,
  });
  
  await logEmailSent(organization.id, booking.id, booking.customer_email);
}
```

### Files to Create/Modify
```
Database:
- supabase/migrations/202605XX_organization_email_templates.sql

Backend:
- src/app/api/organizations/[orgId]/email-templates/route.ts
- src/app/api/organizations/[orgSlug]/email-templates/route.ts
- src/lib/email/send-booking-confirmation.ts (update to use templates)
- src/lib/email/templates/booking-confirmation.mjml (new MJML template)
- src/lib/email/render-template.ts (Handlebars rendering)

Frontend:
- src/app/[locale]/settings/organizations/[orgId]/email-templates/page.tsx

Tests:
- src/__tests__/lib/email/booking-confirmation.test.ts
```

---

## Risks & Mitigations

### CRITICAL

**R1: Email Spoofing - from_email Domain Ownership Not Verified**
- **Severity:** CRITICAL
- **Probability:** High
- **Impact:** Phishing attacks, brand hijacking, legal liability
- **Description:** Admin can set `from_email` to "noreply@yourcompetitor.com" without verification. Lodgra would then send emails appearing to come from competitor, enabling phishing or brand impersonation.
- **Mitigation:**
  - Require DKIM/SPF verification for custom from_email domains
  - Admin uploads SPF/DKIM record proof (text file or screenshot) before custom from_email is enabled
  - Default: only allow `noreply@{orgSlug}.lodgra.io` (verified at signup)
  - If custom domain required: Vercel SendGrid integration must validate domain ownership via DNS TXT record
- **Checklist:**
  - [ ] Custom from_email requires domain verification
  - [ ] SPF/DKIM records checked before enabling
  - [ ] Default from_email is organization-owned subdomain
  - [ ] Test spoofing attempt rejected (e.g., "noreply@fake.com" blocked)

**R2: GDPR Compliance - Unsubscribe & PII Handling**
- **Severity:** CRITICAL
- **Probability:** High (if not handled)
- **Impact:** GDPR fines (4-20% of revenue), legal exposure, user rights violation
- **Description:** Booking confirmation emails must include unsubscribe link (GDPR requirement). Customer email and personal data must not be stored longer than necessary. Email audit logs could expose PII.
- **Mitigation:**
  - Every email MUST include unsubscribe link in footer
  - Unsubscribe link: `https://lodgra.io/unsubscribe?token={signed-jwt}`
  - Email_sent logs: do NOT store customer name, phone, payment details (only: customer_email, sent_at, status)
  - Retention policy: Delete email_sent records older than 90 days
  - Admin dashboard hides PII from logs (shows only email_count, delivery_rate)
- **Checklist:**
  - [ ] Unsubscribe link included in every email template
  - [ ] Email_sent table has RLS policy (only org admin can view)
  - [ ] PII fields not stored in email logs
  - [ ] 90-day retention policy enforced
  - [ ] GDPR audit pass: unsubscribe works, PII protected

### HIGH

**R3: Email Delivery Failures - No Retry Logic**
- **Severity:** HIGH
- **Probability:** High
- **Impact:** Lost bookings, customer frustration, business loss
- **Description:** If SendGrid/email service is temporarily down during booking confirmation, email is lost with no retry. Customer never receives confirmation.
- **Mitigation:**
  - Implement exponential backoff retry logic: 3 retries with delays (5s, 30s, 5min)
  - Store email_sent record BEFORE sending (set status='pending')
  - Background job polls pending emails every minute, retries failures
  - Alert Ops if email_sent.status='failed' after 3 retries
- **Checklist:**
  - [ ] Retry logic: 3 attempts with exponential backoff
  - [ ] Email_sent record created before sending (status='pending')
  - [ ] Background job re-attempts failed emails
  - [ ] Failed emails after 3 retries logged and alerted
  - [ ] Load test: email service down, verify retries succeed when service recovers

**R4: Email Rendering Issues Across Clients**
- **Severity:** HIGH
- **Probability:** Medium
- **Impact:** Broken emails, poor UX, lost conversion
- **Description:** MJML templates may render differently in Outlook, Gmail, Apple Mail (different CSS support, mobile rendering, image handling).
- **Mitigation:**
  - Test email rendering in 5 clients: Gmail, Outlook (web), Apple Mail, Yahoo, mobile (iOS)
  - Use MJML best practices: avoid complex CSS, use table-based layout
  - Preload fallback fonts (system fonts)
  - Test with real-world data (long property names, multi-line addresses)
- **Checklist:**
  - [ ] Email rendering tested in Gmail, Outlook, Apple Mail
  - [ ] Mobile rendering tested (Gmail mobile, Apple Mail mobile)
  - [ ] All links clickable, images load in all clients
  - [ ] Text content is readable even without CSS

**R5: Rate Limiting on Email Sends**
- **Severity:** HIGH
- **Probability:** Medium
- **Impact:** Email queue overflow, service degradation
- **Description:** Malicious actor or bug could trigger 1000s of email sends per second, overwhelming SendGrid quota or mailboxes.
- **Mitigation:**
  - Rate limit: 100 emails/min per organization
  - Burst limit: max 10 emails/second (prevents queue overflow)
  - Per-customer limit: max 5 emails/day to same customer (prevents spam)
  - Return 429 Too Many Requests when limit exceeded
  - Alert Ops if org approaches 80% of daily quota
- **Checklist:**
  - [ ] Per-org rate limit enforced: 100/min
  - [ ] Burst limit enforced: 10/sec
  - [ ] Per-customer limit enforced: 5/day
  - [ ] Test: send 150 emails/min → 51st email rejected with 429

### MEDIUM

**R6: Template Variable Injection - Missing Sanitization**
- **Severity:** MEDIUM
- **Probability:** Medium
- **Impact:** HTML injection, rendering breaks, UX degradation
- **Description:** If `{{propertyName}}` contains HTML characters (e.g., "Beach & Resort <luxury>"), could break email layout or inject HTML.
- **Mitigation:**
  - Sanitize all template variables before rendering (HTML escape: `&`, `<`, `>`, quotes)
  - Use Handlebars built-in escaping: `{{variable}}` (always escapes HTML)
  - Triple-brace syntax `{{{variable}}}` only for explicitly safe HTML
  - Test: property name with "<script>" → verify escaped in email
- **Checklist:**
  - [ ] Template variables sanitized (HTML escaped)
  - [ ] Test XSS payload in property name → escaped in email
  - [ ] Triple-brace syntax never used for user-generated content

**R7: Validation of Subject & Message Length**
- **Severity:** MEDIUM
- **Probability:** Low
- **Impact:** Email truncation, poor UX
- **Description:** If subject is 500 chars (AC5 says max 100), email clients truncate subject. If message is 10K chars, renders poorly.
- **Mitigation:**
  - API validates: subject max 100 chars, message max 5000 chars
  - Admin dashboard shows character counter and live preview
  - Return 400 with error if exceeds limit
- **Checklist:**
  - [ ] Subject length validated (max 100 chars)
  - [ ] Message length validated (max 5000 chars)
  - [ ] Admin dashboard shows char counter
  - [ ] Test: 101-char subject rejected with 400

**R8: Performance - Email Template Rendering**
- **Severity:** MEDIUM
- **Probability:** Low
- **Impact:** Slow email sends, delayed delivery
- **Description:** If MJML rendering takes 5+ seconds per email (complex template, slow regex), send 100 emails takes 8+ minutes.
- **Mitigation:**
  - Pre-compile MJML template at startup (not on every email)
  - Render in < 100ms per email (benchmark with MJML CLI)
  - Use async rendering: render in background job, not in booking confirmation request
- **Checklist:**
  - [ ] MJML template pre-compiled at startup
  - [ ] Render time benchmarked: < 100ms per email
  - [ ] Rendering async (background job or queue)

### LOW

**R9: PII in Email Audit Logs**
- **Severity:** LOW
- **Probability:** Low
- **Impact:** Privacy exposure if logs compromised
- **Description:** Email_sent table could be viewed by ops, potentially exposing customer email addresses.
- **Mitigation:**
  - Email_sent RLS policy: only org admin can view own org's logs
  - Ops dashboard redacts email addresses (shows only domain: "*@gmail.com")
  - Logs retained only 90 days
- **Checklist:**
  - [ ] Email_sent table has RLS policy limiting to org members
  - [ ] Ops dashboard redacts email addresses
  - [ ] Logs auto-deleted after 90 days

**R10: Email Tracking & Analytics Implementation**
- **Severity:** LOW
- **Probability:** Low
- **Impact:** Feature limitation, not critical
- **Description:** Admin may want to see email open rates, click tracking (SendGrid supports this). Feature not in AC8 but might be expected by users.
- **Mitigation:**
  - Document in admin dashboard: "Email tracking not currently supported (roadmap)"
  - If tracking needed: SendGrid webhook integration required (future story)
- **Checklist:**
  - [ ] Admin dashboard clarifies: "Email delivery status only (opens/clicks not tracked)"
  - [ ] Roadmap documented for future tracking feature

---

## Testing Plan

### QA Test Cases

- [ ] **TC1:** Admin sets custom subject and verifies it appears in email
- [ ] **TC2:** Admin sets custom from_email and verifies email comes from that address
- [ ] **TC3:** Admin uploads custom message with {{propertyName}} variable and verifies it's replaced
- [ ] **TC4:** Company logo appears in email header (if configured)
- [ ] **TC5:** Email button uses company primary color
- [ ] **TC6:** Email renders correctly in Gmail, Outlook, Apple Mail (manual check)
- [ ] **TC7:** Email is responsive on mobile devices
- [ ] **TC8:** Test email can be sent to admin
- [ ] **TC9:** Email delivery logged in email_sent table
- [ ] **TC10:** Organization without template receives default Lodgra email

---

## QA Results

### Gate Decision: **FAIL** ❌

**Status:** **CRITICAL SECURITY GAPS + INCOMPLETE AC block approval.** Return to @dev with blocking issues. Multiple security mitigations unimplemented; admin dashboard missing; email provider integration placeholder.

---

### Acceptance Criteria Assessment

| AC | Status | Evidence | Issues |
|----|--------|----------|--------|
| AC1: Email template table | ✅ PASS | supabase/migrations/20260522020000_organization_email_templates.sql created with all fields, RLS enforced | Schema solid |
| AC2: Admin API | ✅ PASS | PATCH /api/organizations/[orgId]/email-templates with validation (subject max 100, message max 5000, from_email format) | Validation logic present |
| AC3: Public fetch | ✅ PASS | GET /api/organizations/[orgSlug]/email-templates returns template + branding merged | Cache headers set |
| AC4: Dynamic email rendering | ⚠️ **INCOMPLETE** | MJML template created, Handlebars rendering present, BUT sendEmailViaProvider() is **placeholder with TODO comment** — actual provider integration (SendGrid/Resend) not implemented | **BLOCKER:** Email not actually sent. AC requirement to send branded emails unfulfilled |
| AC5: Template variables | ✅ PASS | MJML template supports {{customerName}}, {{propertyName}}, {{checkInDate}}, {{checkOutDate}}, {{totalPrice}}, {{currency}} | Handlebars substitution working |
| AC6: Admin dashboard | ❌ **NOT CREATED** | File list shows `[ ] src/app/[locale]/settings/organizations/[orgId]/email-templates/page.tsx` | **BLOCKER:** Required for AC6. Admin has no UI to configure email templates. Cannot verify form, fields, preview, save, test email |
| AC7: Email tracking | ✅ PASS | email_sent table created with logging (organization_id, booking_id, customer_email, template_type, status, sent_at, retry_count) | RLS enforced |
| AC8: Backward compatibility | ✅ PASS | Defaults applied when no template | No regression expected |

**Total: 5/8 AC complete. 3 AC incomplete/blocked.**

---

### Risk Mitigation Verification

| Risk | Severity | Mitigation Spec | Status | Issues |
|------|----------|-----------------|--------|--------|
| R1: Email spoofing | **CRITICAL** | "Require DKIM/SPF verification for custom from_email" + default to `noreply@{orgSlug}.lodgra.io` | ❌ **NOT IMPLEMENTED** | Code only validates format regex, does NOT prevent spoofing. No domain verification, no SPF/DKIM check. Admin can set `from_email: "noreply@competitor.com"` and Lodgra will send from it. **SECURITY HOLE.** |
| R2: GDPR compliance | **CRITICAL** | "Unsubscribe link with signed JWT" + "90-day retention policy" | ⚠️ **PARTIAL** | ✅ Unsubscribe link present in template. ❌ Token is plain base64(`org_id:email`), NOT signed JWT. ❌ No signature verification = replay attack risk. ❌ 90-day retention policy documented as TODO (not implemented). |
| R3: Email delivery failures | **HIGH** | "Implement exponential backoff retry: 3 retries with delays (5s, 30s, 5min)" | ❌ **NOT IMPLEMENTED** | sendEmailViaProvider() is placeholder. No retry logic. If SendGrid down, customer gets no confirmation and no auto-retry. |
| R4: Email rendering | **HIGH** | "Test in Gmail, Outlook, Apple Mail, Yahoo, mobile" | ⚠️ **ASSUMED** | MJML template created but no cross-client testing documented. No test results for Outlook/Apple Mail rendering. |
| R5: Rate limiting | **HIGH** | "Per-org: 100/min, per-customer: 5/day, burst: 10/sec" + 429 response | ❌ **NOT IMPLEMENTED** | No rate limiting code visible in API routes. Missing Upstash/Redis integration. Malicious actor could trigger 1000s of emails. |
| R6: Template variable sanitization | **MEDIUM** | "HTML escape all variables" | ✅ IMPLEMENTED | htmlEscape() function in render-template.ts sanitizes `&`, `<`, `>`, quotes |
| R7: Subject/message length validation | **MEDIUM** | "subject max 100, message max 5000, char counter in UI" | ⚠️ **PARTIAL** | ✅ API validation present. ❌ Admin dashboard doesn't exist, so char counter UI not implemented. |
| R8: Performance — template rendering | **MEDIUM** | "Pre-compile MJML at startup, render < 100ms" | ✅ IMPLEMENTED | Template compiled once at startup (getCompiledTemplate caches result). Async rendering approach correct. |
| R9: PII in logs | **LOW** | "RLS policy + ops redaction + 90-day retention" | ⚠️ **PARTIAL** | ✅ RLS policy enforced. ❌ No ops dashboard redaction (not implemented). ❌ 90-day retention policy documented as TODO. |
| R10: Email tracking analytics | **LOW** | "Document as not supported (roadmap)" | ⚠️ **PARTIAL** | No analytics implemented. Not documented in code/dashboard. |

---

### Critical Security Issues

**P0 (Security Blockers):**

1. **R1: Email Spoofing Not Mitigated** ❌
   - **Problem:** Admin can set `from_email` to ANY domain without verification
   - **Risk:** Phishing attacks, brand hijacking, legal liability
   - **Current code:** Only regex validation: `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
   - **Required fix:** 
     - Add SPF/DKIM domain verification before allowing custom from_email
     - Default to `noreply@{orgSlug}.lodgra.io` only
     - Admin must upload SPF/DKIM proof or use Vercel SendGrid to verify DNS
   - **Action:** @dev must implement domain verification gate before AC4 email sending

2. **R2: Unsubscribe Token Not Signed** ❌
   - **Problem:** Token is plain `base64(org_id:email)` — no signature, no expiration
   - **Risk:** Replay attacks, forged unsubscribe requests
   - **Current code:** `Buffer.from(payload).toString('base64')`
   - **Required fix:**
     - Generate signed JWT: `HMAC_SHA256(org_id:email, secret_key)`
     - Add expiration (30 days)
     - Verify signature on unsubscribe endpoint
   - **Action:** @dev must implement proper JWT signing

3. **R3: Email Provider Integration Missing** ❌
   - **Problem:** sendEmailViaProvider() is placeholder with TODO comment
   - **Risk:** Emails not actually sent; AC4 unfulfilled
   - **Current code:** Logs payload to console only
   - **Required fix:**
     - Implement SendGrid or Resend integration
     - Retry logic: 3 attempts with exponential backoff
     - Store email_sent record BEFORE sending (status='pending')
   - **Action:** @dev must complete provider integration

4. **R5: No Rate Limiting** ❌
   - **Problem:** No rate limits on email sends
   - **Risk:** DoS via 1000s of emails per second
   - **Current code:** Endpoint has no throttling
   - **Required fix:**
     - Implement Upstash Redis rate limiting
     - Per-org: 100/min, Per-customer: 5/day, Burst: 10/sec
     - Return 429 when exceeded
   - **Action:** @dev must add rate limiting middleware

---

### AC Implementation Gaps

**P0 (Blockers):**

1. **AC4 Incomplete:** sendEmailViaProvider() placeholder
   - Cannot send emails; entire feature non-functional

2. **AC6 Missing:** Admin dashboard not created
   - No UI for admins to configure email templates
   - All template configuration blocked

**P1 (Must Fix):**

1. **No component tests** for admin dashboard form
2. **No cross-client email tests** (R4 verification)
3. **No manual QA execution** (TC1-TC10 not documented)
4. **90-day retention policy** not implemented (R2)

---

### Code Quality Assessment

**Strengths:**
- ✅ MJML template well-structured, responsive, variable support
- ✅ Database schema sound (FKs, RLS, appropriate fields)
- ✅ Handlebars rendering with variable substitution
- ✅ HTML sanitization prevents injection

**Critical Gaps:**
- ❌ Email provider integration placeholder (sendEmailViaProvider)
- ❌ No retry logic for failed sends
- ❌ No rate limiting
- ❌ Admin dashboard missing entirely
- ❌ Domain verification not implemented
- ❌ Unsubscribe token not signed

**Missing Tests:**
- ❌ Component tests for admin form
- ❌ Email rendering cross-client tests
- ❌ Retry logic tests
- ❌ Rate limiting tests
- ❌ Manual QA test execution (TC1-TC10)

---

### Verdict Rationale

**GATE DECISION: FAIL ❌**

This story is **not ready for release** due to:

1. **Critical security gaps** (R1, R2) that create liability
2. **Email provider integration missing** — core feature non-functional (AC4)
3. **Admin dashboard missing** — no way to configure templates (AC6)
4. **No retry logic** — failed emails lost (R3)
5. **No rate limiting** — DoS vulnerability (R5)
6. **Incomplete test coverage** — no manual QA results

**Impact:** Customers cannot receive branded emails; security vulnerabilities prevent deployment.

---

### Required Fixes for Approval

**CRITICAL (blocks merge):**
1. ✅ Implement email provider (SendGrid/Resend) with retry logic (AC4 + R3)
2. ✅ Implement domain verification for custom from_email (R1 mitigation)
3. ✅ Sign unsubscribe JWT token + verify on unsubscribe (R2 fix)
4. ✅ Create admin dashboard page with form, preview, test email (AC6)
5. ✅ Add rate limiting middleware (R5 mitigation)

**HIGH (required before merge):**
1. ✅ Component tests for admin form
2. ✅ Cross-client email rendering tests
3. ✅ Manual QA test execution (TC1-TC10)
4. ✅ Implement 90-day retention policy (R2 mitigation)

**MEDIUM (recommended):**
1. Add email delivery status dashboard
2. Document unsubscribe workflow for users
3. Add ops monitoring for bounce rates

---

### Recommendation for @dev

**Story Status:** Return to In Progress with blocking feedback

**Tasks for Completion:**
1. Implement SendGrid API integration in sendEmailViaProvider()
2. Add 3-retry exponential backoff logic
3. Implement domain verification gate for custom from_email
4. Sign unsubscribe token with JWT
5. Create admin dashboard page (form fields, preview, test send, save)
6. Add rate limiting via Upstash
7. Create all required tests
8. Execute manual QA test cases

**Estimated effort:** 8-12 hours (blocking on provider API integration)

---

### CodeRabbit Integration Check
- ⚠️ Pre-commit review STRONGLY RECOMMENDED
- Focus areas: sendEmailViaProvider placeholder, domain verification logic, retry implementation, rate limiting middleware

---

## Change Log

| Date | Status | Change | Agent |
|------|--------|--------|-------|
| 2026-05-22 | Ready → InProgress | Core infrastructure complete: Database (organization_email_templates + email_sent), API endpoints (admin CRUD + public fetch), MJML template with variables, rendering logic (Handlebars + sanitization), booking sender. Pending: admin dashboard, test email endpoint, provider integration. | @dev |
| 2026-05-22 | Ready | Risks & Mitigations incorporated (10 risks: 2 CRITICAL, 3 HIGH, 4 MEDIUM, 1 LOW) — Ready for development | @po |
| 2026-05-22 | Draft | Story created for Phase 2 email customization | @sm |

---

## File List

**(Updated by @dev as work progresses)**

```
Database:
- [x] supabase/migrations/20260522020000_organization_email_templates.sql

Backend:
- [x] src/app/api/organizations/[orgId]/email-templates/route.ts (admin CRUD)
- [x] src/app/api/organizations/[orgSlug]/email-templates/route.ts (public fetch with branding)
- [x] src/lib/email/send-booking-confirmation.ts (integration with templates + branding)
- [x] src/lib/email/templates/booking-confirmation.mjml (MJML template with variables)
- [x] src/lib/email/render-template.ts (Handlebars + MJML rendering, sanitization)

Frontend:
- [ ] src/app/[locale]/settings/organizations/[orgId]/email-templates/page.tsx (admin dashboard)

Tests:
- [ ] src/__tests__/lib/email/booking-confirmation.test.ts

Pending Integration:
- [ ] Integration with booking confirmation flow
- [ ] Email provider setup (SendGrid/Resend)
- [ ] Test email sender endpoint
```

---

## Blockers

- Depends on Story 31.2 (Branding API) for colors and logo

---

## Dependencies

- **Predecessor:** Story 31.1, 31.2
- **Parallel:** Story 31.3
