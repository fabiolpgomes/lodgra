# Story 25.1: Migração DB — tabela `property_reviews` + RLS + índices

**Epic:** 25 — OTA Reviews: Social Proof na Página de Reserva Direta
**Status:** Ready for Review
**Prioridade:** P1 — Fundação: todas as stories 25.x dependem disto
**Tipo:** Backend / Database
**Agente:** @dev

---

## Story

**Como** sistema,
**Quero** ter uma tabela `property_reviews` com RLS correctamente configurada,
**Para que** o gestor possa adicionar reviews curadas de OTAs e a página pública possa exibi-las com segurança.

---

## Acceptance Criteria

- [ ] **AC1** — Tabela `property_reviews` criada com todos os campos definidos no epic
- [ ] **AC2** — Restrição `CHECK` no campo `source` (valores válidos: `'booking'`, `'airbnb'`, `'google'`, `'tripadvisor'`, `'direct'`, `'other'`)
- [ ] **AC3** — Restrição `CHECK` no campo `rating` (1.0 ≤ rating ≤ 10.0)
- [ ] **AC4** — Restrição `CHECK` no campo `review_text` (max 500 caracteres, nullable)
- [ ] **AC5** — RLS activado: SELECT público (`USING (true)`), INSERT/UPDATE/DELETE restritos à org do utilizador autenticado
- [ ] **AC6** — Índice composto `(property_id, is_featured)` para queries da página pública
- [ ] **AC7** — Índice em `organization_id` para queries admin
- [ ] **AC8** — Tipos TypeScript adicionados em `src/types/database.ts`: `ReviewSource`, `PropertyReview`
- [ ] **AC9** — Migração aplicável sem erros em Supabase local (`supabase db reset` ou `supabase migration up`)

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> A validação de qualidade usará processo de revisão manual.

---

## Tasks / Subtasks

- [x] **Task 1** — Criar ficheiro de migração SQL (AC: 1, 2, 3, 4, 5, 6, 7)
  - [x] 1.1 Criar `supabase/migrations/20260510_01_property_reviews.sql`
  - [x] 1.2 Escrever `CREATE TABLE IF NOT EXISTS property_reviews` com todos os campos
  - [x] 1.3 Adicionar restrições CHECK: `source`, `rating`, `review_text`
  - [x] 1.4 `ALTER TABLE property_reviews ENABLE ROW LEVEL SECURITY`
  - [x] 1.5 Criar policy `property_reviews_select` → `FOR SELECT USING (true)`
  - [x] 1.6 Criar policy `property_reviews_insert` → `FOR INSERT WITH CHECK (organization_id = ...)`
  - [x] 1.7 Criar policy `property_reviews_update` → `FOR UPDATE USING (organization_id = ...)`
  - [x] 1.8 Criar policy `property_reviews_delete` → `FOR DELETE USING (organization_id = ...)`
  - [x] 1.9 Criar índice composto `idx_property_reviews_property_featured` em `(property_id, is_featured)`
  - [x] 1.10 Criar índice `idx_property_reviews_org_id` em `(organization_id)`

- [x] **Task 2** — Adicionar tipos TypeScript (AC: 8)
  - [x] 2.1 Abrir `src/types/database.ts`
  - [x] 2.2 Adicionar `export type ReviewSource = 'booking' | 'airbnb' | 'google' | 'tripadvisor' | 'direct' | 'other'`
  - [x] 2.3 Adicionar `export type PropertyReview` com todos os campos da tabela
  - [x] 2.4 Verificar que não há conflitos com tipos existentes

- [x] **Task 3** — Validar migração (AC: 9)
  - [x] 3.1 Correr `npm run typecheck` — passou sem erros (`npx tsc --noEmit`)
  - [x] 3.2 Correr `npm run lint` — ficheiros alterados sem erros (4 erros pré-existentes em ficheiros não tocados)
  - [x] 3.3 Verificar SQL sintaticamente correcto (sem erros de parsing)

---

## Dev Notes

### Stack Técnica Relevante
[Source: docs/architecture/brownfield-architecture.md]
- Base de dados: Supabase (PostgreSQL)
- Framework: Next.js 15, TypeScript 5.x
- Tipos centralizados em `src/types/database.ts`

### Convenção de Nomes de Migração
[Source: supabase/migrations/ — convenção observada]
```
YYYYMMDD_NN_descricao.sql
```
Ficheiro a criar: `supabase/migrations/20260510_01_property_reviews.sql`

### Schema Completo da Tabela

```sql
CREATE TABLE IF NOT EXISTS property_reviews (
  id              uuid        PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id uuid        NOT NULL REFERENCES organizations(id),
  property_id     uuid        NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
  source          text        NOT NULL CHECK (source IN ('booking', 'airbnb', 'google', 'tripadvisor', 'direct', 'other')),
  reviewer_name   text        NOT NULL,
  rating          numeric(3,1) NOT NULL CHECK (rating >= 1.0 AND rating <= 10.0),
  review_text     text        CHECK (review_text IS NULL OR char_length(review_text) <= 500),
  review_date     date        NOT NULL,
  is_featured     boolean     NOT NULL DEFAULT false,
  created_at      timestamptz DEFAULT now(),
  updated_at      timestamptz DEFAULT now()
);
```

### Padrão RLS da Tabela (Multi-tenancy)
[Source: supabase/migrations/20260504_01_expense_documents.sql — padrão org isolation]
[Source: supabase/migrations/20260506_01_property_amenities_rooms_fees.sql — padrão SELECT público]

Tabela `property_reviews` tem `organization_id` directamente. Usar padrão:

```sql
-- SELECT público (página pública não requer autenticação para ler reviews)
CREATE POLICY "property_reviews_select" ON property_reviews
  FOR SELECT USING (true);

-- INSERT restrito à org do utilizador
CREATE POLICY "property_reviews_insert" ON property_reviews
  FOR INSERT WITH CHECK (
    organization_id = (
      SELECT organization_id FROM user_profiles WHERE id = auth.uid()
    )
  );

-- UPDATE restrito à org do utilizador
CREATE POLICY "property_reviews_update" ON property_reviews
  FOR UPDATE USING (
    organization_id = (
      SELECT organization_id FROM user_profiles WHERE id = auth.uid()
    )
  );

-- DELETE restrito à org do utilizador
CREATE POLICY "property_reviews_delete" ON property_reviews
  FOR DELETE USING (
    organization_id = (
      SELECT organization_id FROM user_profiles WHERE id = auth.uid()
    )
  );
```

### Índices

```sql
-- Query da página pública: reviews destaque por propriedade
CREATE INDEX IF NOT EXISTS idx_property_reviews_property_featured
  ON property_reviews (property_id, is_featured);

-- Query admin: reviews por organização
CREATE INDEX IF NOT EXISTS idx_property_reviews_org_id
  ON property_reviews (organization_id);
```

### Tipos TypeScript a Adicionar
[Source: src/types/database.ts — padrão observado em PropertyRoom, PropertyBathroom]

```typescript
export type ReviewSource = 'booking' | 'airbnb' | 'google' | 'tripadvisor' | 'direct' | 'other'

export type PropertyReview = {
  id: string
  organization_id: string
  property_id: string
  source: ReviewSource
  reviewer_name: string
  rating: number
  review_text: string | null
  review_date: string       // ISO date string 'YYYY-MM-DD'
  is_featured: boolean
  created_at: string
  updated_at: string
}
```

Adicionar a seguir às outras types de propriedade (depois de `PropertyBathroom`, antes de `Platform`).

### Ficheiros a Criar/Modificar

| Acção | Ficheiro |
|-------|---------|
| CRIAR | `supabase/migrations/20260510_01_property_reviews.sql` |
| MODIFICAR | `src/types/database.ts` |

### Nota de Segurança

O SELECT com `USING (true)` é intencional — reviews são dados públicos exibidos na página de reserva directa sem autenticação. As operações de escrita (INSERT/UPDATE/DELETE) ficam sempre restritas à organização autenticada.

### Testing

- Não são necessários testes unitários para esta story (apenas migração SQL + tipos)
- Verificação: `npm run typecheck` e `npm run lint` devem passar sem erros
- Migração deve ser idempotente: usar `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`, `CREATE POLICY IF NOT EXISTS` ou blocos `DO $$ BEGIN ... END $$`

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-10 | 1.0 | Story criada | River (@sm) |

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Debug Log References
N/A — implementação directa, sem issues de debugging.

### Completion Notes List
- Migração SQL criada com `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS` — idempotente
- SELECT público (`USING (true)`) intencional: página de reserva não requer auth
- 4 erros de lint pré-existentes em `BrazilLanding.tsx` e `rateLimit.ts` — não introduzidos por esta story
- `npx tsc --noEmit` passou sem erros

### File List
| Acção | Ficheiro |
|-------|---------|
| CRIADO | `supabase/migrations/20260510_01_property_reviews.sql` |
| MODIFICADO | `src/types/database.ts` |
| MODIFICADO | `docs/stories/25.1.story.md` |
