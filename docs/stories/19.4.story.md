# Story 19.4: Disponibilidade Pública — Bloquear Datas Reservadas

**Epic:** 19 — Google Vacation Rentals Distribution (Fase 1 — SEO-Ready)
**Status:** Done
**Prioridade:** P1 — Hóspedes não devem conseguir seleccionar datas já reservadas
**Tipo:** Full Stack (query + UI ambos os widgets)
**Agente:** @dev
**Depende de:** 19.1 ✅

---

## Story

**Como** hóspede a consultar uma propriedade pública,
**Quero** ver quais datas estão indisponíveis no formulário de reserva,
**Para que** não perca tempo a tentar reservar datas já ocupadas.

---

## Acceptance Criteria

- [x] **AC1** — Page `/p/[slug]` query reservas `confirmed` + `pending` dos próximos 365 dias
- [x] **AC2** — `blockedRanges` passados via `PropertyPageV2` → `BookingWidgetDesktop` + `BookingWidgetMobile`
- [x] **AC3** — Se check-in seleccionado cair dentro de uma reserva existente → erro inline "Data indisponível"
- [x] **AC4** — Se o período [check-in, check-out] sobrepuser qualquer reserva existente → erro inline "Período contém datas reservadas"
- [x] **AC5** — Botão "Reservar" desactivado enquanto houver erro de sobreposição
- [x] **AC6** — Build PASS + Lint PASS

---

## Dev Notes

### Query na page.tsx (reservas futuras confirmadas)

```ts
const { data: blockedReservations } = await adminClient
  .from('reservations')
  .select('start_date, end_date')
  .eq('property_id', property.id)
  .in('status', ['confirmed', 'pending'])
  .gte('end_date', today)
  .lte('start_date', futureDate)
```

### Lógica de sobreposição

```ts
// Data bloqueada: check-in cai dentro de uma reserva
function isDateBlocked(date: string, ranges: BlockedRange[]): boolean {
  return ranges.some(r => date >= r.start && date < r.end)
}

// Período sobrepõe-se: qualquer overlap [ci, co) ∩ [start, end)
function isOverlapping(ci: string, co: string, ranges: BlockedRange[]): boolean {
  return ranges.some(r => ci < r.end && co > r.start)
}
```

### Ficheiros a Modificar

```
src/app/p/[slug]/page.tsx                        ← query + pass blockedRanges
src/components/common/public/PropertyPageV2.tsx  ← prop + forward para ambos widgets
src/components/common/public/booking/BookingWidgetDesktop.tsx  ← validação + erro
src/components/common/public/booking/BookingWidgetMobile.tsx   ← validação + erro
```

---

## Tasks

- [x] 1. Adicionar query de reservas bloqueadas na page.tsx
- [x] 2. Passar `blockedRanges` via PropertyPageV2 → ambos widgets
- [x] 3. Adicionar validação e erro no `BookingWidgetDesktop`
- [x] 4. Adicionar validação e erro no `BookingWidgetMobile`
- [x] 5. Build PASS + Lint PASS

---

## File List

```
src/app/p/[slug]/page.tsx                                      ← modified
src/components/common/public/PropertyPageV2.tsx                ← modified
src/components/common/public/booking/BookingWidgetDesktop.tsx  ← modified
src/components/common/public/booking/BookingWidgetMobile.tsx   ← modified
docs/stories/19.4.story.md                                     ← created
```

## Dev Agent Record

### Status: Done
### Agent Model Used: claude-sonnet-4-6

### Completion Notes
- blockedRanges usa half-open interval [start, end) — check-out day não é ocupado
- Erro de sobreposição bloqueia botão "Reservar" em ambos os widgets
- Sem nova dependência — lógica pura com date-fns já instalado

### Change Log
- 2026-05-07: Story 19.4 — disponibilidade pública bloqueada (Dex, claude-sonnet-4-6)

---

## QA Results

### Review Date: 2026-05-07
### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementação correcta e completa. isDateBlocked usa half-open interval [start, end) — checkout day não bloqueado. isRangeOverlapping garante AC4. Ambas as funções helper definidas fora do componente (sem recriação). Disponibilidade verificada APÓS effectiveMinNights (ordem correcta conforme requisito). checkInError e checkOutError são estados independentes. Botão "Reservar" desactivado em qualquer erro de sobreposição. Desktop e Mobile têm lógica idêntica. 0 erros TypeScript, 0 erros ESLint.

### Compliance Check
- All ACs Met: ✓ AC1–AC6 todos
- Ordering: ✓ availability check AFTER pricing-rule check
- Half-open interval: ✓ [start, end) — checkout day livre
- No new deps: ✓ pure date-string comparison

### Gate Status
Gate: PASS (100/100) → docs/qa/gates/19.4-public-availability-blocked.yml

### Recommended Status
[✓ Ready for Done]
