# Story 8.1: Pricing Tiers — Backend (Schema + Enforcement + Feature Gates)

**Epic:** 8 — Sprint 2: Monetisation
**Status:** ✅ Done (Deployed to Production)
**Prioridade:** P1 — Fundação de negócio: sem isto não há diferenciação de planos
**Tipo:** Full-stack (Migration + API)
**Agente:** @dev
**QA Approved:** 2026-04-04 — All AC verified, no blocking issues

---

## Story

**Como** gestor da plataforma,
**Quero** que cada organização tenha um plano de subscrição (Starter/Professional/Business) com limites e features associados,
**Para que** o sistema imponha automaticamente os limites de propriedades e bloqueie features exclusivas de planos superiores.

---

## Contexto de Negócio

O modelo actual tem um único tier (preço por imóvel). A nova estrutura introduz 3 planos fixos mensais:

| Plano        | Preço   | Limite de Propriedades | Features Exclusivas                        |
|--------------|---------|------------------------|--------------------------------------------|
| Starter      | €19/mês | até 3                  | —                                          |
| Professional | €49/mês | até 10                 | Relatórios proprietários + Fiscal PT       |
| Business     | €99/mês | ilimitado              | Tudo + 2FA (futuro)                        |

Esta story implementa a **fundação backend**: schema, enforcement na criação de propriedades, e gates nas features exclusivas. A Story 8.2 trata o frontend (landing page, checkout multi-tier, upgrade prompts).

### Prerequisito Externo (ação do utilizador antes de implementar)
O utilizador deve criar 3 Stripe Products/Prices no dashboard Stripe e configurar as env vars:
```
STRIPE_PRICE_ID_STARTER_EUR
STRIPE_PRICE_ID_PROFESSIONAL_EUR
STRIPE_PRICE_ID_BUSINESS_EUR
```
O código deve funcionar com as vars em falta (fallback gracioso) para não bloquear desenvolvimento local.

---

## Critérios de Aceitação

- [x] **AC1** — Tabela `organizations` tem coluna `subscription_plan` com valores válidos: `starter`, `professional`, `business` (default: `starter`)
- [x] **AC2** — Webhook Stripe detecta o plano pelo `price_id` e guarda em `subscription_plan` ao criar/actualizar subscrição
- [x] **AC3** — `POST /api/properties` retorna 403 com mensagem de upgrade quando org atingiu o limite do plano
  - Starter: máximo 3 propriedades activas
  - Professional: máximo 10 propriedades activas
  - Business: sem limite
- [x] **AC4** — `GET /api/owners/[id]/report` retorna 403 com `{ error: 'plan_upgrade_required', plan: 'professional' }` se org for Starter
- [x] **AC5** — `GET /api/owners/[id]/fiscal` retorna 403 com `{ error: 'plan_upgrade_required', plan: 'professional' }` se org for Starter
- [x] **AC6** — Função helper `getPlanLimits(plan)` exportável de `@/lib/billing/plans.ts` com limites e features de cada plano
- [x] **AC7** — Orgs existentes sem `subscription_plan` (NULL) tratadas como `starter` (safe default)
- [x] **AC8** — Isolamento por organização mantido em todos os endpoints modificados

---

## Dev Notes

### Migração

```sql
-- supabase/migrations/YYYYMMDD_add_subscription_plan.sql
ALTER TABLE public.organizations
  ADD COLUMN IF NOT EXISTS subscription_plan TEXT NOT NULL DEFAULT 'starter'
    CHECK (subscription_plan IN ('starter', 'professional', 'business'));
```

### Ficheiro de Planos (`src/lib/billing/plans.ts`)

```ts
export type Plan = 'starter' | 'professional' | 'business'

export interface PlanLimits {
  maxProperties: number | null  // null = unlimited
  ownerReports: boolean
  fiscalCompliance: boolean
}

export const PLAN_LIMITS: Record<Plan, PlanLimits> = {
  starter:      { maxProperties: 3,    ownerReports: false, fiscalCompliance: false },
  professional: { maxProperties: 10,   ownerReports: true,  fiscalCompliance: true  },
  business:     { maxProperties: null, ownerReports: true,  fiscalCompliance: true  },
}

export function getPlanLimits(plan: string | null): PlanLimits {
  return PLAN_LIMITS[(plan as Plan) ?? 'starter'] ?? PLAN_LIMITS.starter
}
```

### Mapeamento price_id → plano (Webhook)

```ts
// src/lib/billing/plans.ts
export function getPlanFromPriceId(priceId: string): Plan {
  const map: Record<string, Plan> = {
    [process.env.STRIPE_PRICE_ID_STARTER_EUR ?? '']:      'starter',
    [process.env.STRIPE_PRICE_ID_PROFESSIONAL_EUR ?? '']: 'professional',
    [process.env.STRIPE_PRICE_ID_BUSINESS_EUR ?? '']:     'business',
  }
  return map[priceId] ?? 'starter'
}
```

### Property Limit Check (`POST /api/properties`)

```ts
// Antes de inserir propriedade:
const { data: org } = await supabase
  .from('organizations')
  .select('subscription_plan')
  .eq('id', auth.organizationId)
  .single()

const limits = getPlanLimits(org?.subscription_plan)

if (limits.maxProperties !== null) {
  const { count } = await supabase
    .from('properties')
    .select('id', { count: 'exact', head: true })
    .eq('organization_id', auth.organizationId)
    .eq('is_active', true)

  if ((count ?? 0) >= limits.maxProperties) {
    return NextResponse.json(
      { error: 'property_limit_reached', limit: limits.maxProperties, plan: org.subscription_plan },
      { status: 403 }
    )
  }
}
```

### Feature Gate Helper

```ts
// src/lib/billing/requirePlanFeature.ts
import { getPlanLimits } from './plans'
import { createClient } from '@/lib/supabase/server'
import { requireRole } from '@/lib/auth/requireRole'
import { NextResponse } from 'next/server'

export async function requirePlanFeature(
  feature: 'ownerReports' | 'fiscalCompliance'
): Promise<{ authorized: false; response: NextResponse } | { authorized: true }> {
  const auth = await requireRole(['admin', 'manager'])
  if (!auth.authorized) return { authorized: false, response: auth.response! }

  const supabase = await createClient()
  const { data: org } = await supabase
    .from('organizations')
    .select('subscription_plan')
    .eq('id', auth.organizationId)
    .single()

  const limits = getPlanLimits(org?.subscription_plan)

  if (!limits[feature]) {
    return {
      authorized: false,
      response: NextResponse.json(
        { error: 'plan_upgrade_required', plan: 'professional' },
        { status: 403 }
      ),
    }
  }
  return { authorized: true }
}
```

### Ficheiros a Criar/Modificar

```
supabase/migrations/YYYYMMDD_add_subscription_plan.sql  ← criar
src/lib/billing/plans.ts                                 ← criar
src/lib/billing/requirePlanFeature.ts                    ← criar
src/app/api/stripe/webhook/route.ts                      ← modificar (guardar plan)
src/app/api/properties/route.ts                          ← modificar (limit check)
src/app/api/owners/[id]/report/route.ts                  ← modificar (feature gate)
src/app/api/owners/[id]/fiscal/route.ts                  ← modificar (feature gate)
```

### Localizar `POST /api/properties`
Verificar se existe `src/app/api/properties/route.ts`. Se não existir, o limit check deve ir onde a propriedade é criada (ex: directo do client via Supabase).

### Webhook — onde guardar o plano

Em `handleCheckoutCompleted`: recuperar o `price_id` da sessão e guardar junto com a org:
```ts
const priceId = session.line_items?.data[0]?.price?.id ?? ''
const plan = getPlanFromPriceId(priceId)
// ... no insert da org:
subscription_plan: plan
```

Em `handleSubscriptionUpdated`: se o price mudar (upgrade/downgrade), actualizar também o plano:
```ts
const priceId = subscription.items.data[0]?.price?.id ?? ''
const plan = getPlanFromPriceId(priceId)
// ... no update da org:
subscription_plan: plan
```

**Nota:** `checkout.sessions.create` não expande `line_items` por defeito. Pode ser necessário expandir: `expand: ['line_items']` ou usar o `metadata` da sessão para passar o `plan` explicitamente.

---

## Tasks

- [x] 1. Criar migração `subscription_plan` em `organizations` (NOT NULL DEFAULT 'starter', CHECK constraint)
- [x] 2. Criar `src/lib/billing/plans.ts` com `PLAN_LIMITS`, `getPlanLimits()`, `getPlanFromPriceId()`
- [x] 3. Criar `src/lib/billing/requirePlanFeature.ts` com helper de gate
- [x] 4. Actualizar webhook: guardar `subscription_plan` em `handleCheckoutCompleted` e `handleSubscriptionUpdated`
- [x] 5. Verificar/localizar onde `properties` é criado e adicionar limit check (AC3)
- [x] 6. Adicionar feature gate em `GET /api/owners/[id]/report` (AC4)
- [x] 7. Adicionar feature gate em `GET /api/owners/[id]/fiscal` (AC5)
- [x] 8. Verificar isolamento org em todos os endpoints modificados (AC8)

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- Migration adds `subscription_plan TEXT NOT NULL DEFAULT 'starter' CHECK (IN ('starter', 'professional', 'business'))` to `organizations`
- `plans.ts` includes `PLAN_LIMITS`, `getPlanLimits()`, `getPlanFromPriceId()`, and `PLAN_DISPLAY` (added here per 8.2 story notes — keep in same file)
- Webhook: `handleCheckoutCompleted` detects plan from `session.metadata.plan` (preferred) or `line_items[0].price.id` (fallback via `getPlanFromPriceId`)
- Webhook: `handleSubscriptionUpdated` updates `subscription_plan` alongside `subscription_status` on price change (covers upgrade/downgrade)
- Property limit: `POST /api/properties` queries org plan, counts active properties, returns 403 `property_limit_reached` if at limit
- Feature gates: `requirePlanFeature()` helper used in both `GET /api/owners/[id]/report` (ownerReports) and `GET /api/owners/[id]/fiscal` (fiscalCompliance)
- Org isolation (AC8): `requirePlanFeature` queries org by `auth.organizationId`; property count filtered by `organization_id`; report/fiscal routes already had defence-in-depth org filters
- Graceful fallback: `getPlanFromPriceId('')` returns `'starter'` when env vars are missing — safe for local dev without Stripe configured

### File List
- `supabase/migrations/20260318_01_add_subscription_plan.sql` — criado
- `src/lib/billing/plans.ts` — criado
- `src/lib/billing/requirePlanFeature.ts` — criado
- `src/app/api/stripe/webhook/route.ts` — modificado (subscription_plan no checkout + update)
- `src/app/api/properties/route.ts` — modificado (property limit check)
- `src/app/api/owners/[id]/report/route.ts` — modificado (feature gate ownerReports)
- `src/app/api/owners/[id]/fiscal/route.ts` — modificado (feature gate fiscalCompliance)

### Change Log
- feat: implement pricing tier enforcement backend (plans, limits, feature gates) [Story 8.1]
