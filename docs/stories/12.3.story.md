# Story 12.3: Stripe Booking Payments — Connect + Payment Processing

**Epic:** 12 — Stripe SaaS + Multi-Tenant Payments
**Status:** Ready
**Prioridade:** P0 — Core direct booking feature
**Tipo:** Full-stack (Backend API + Frontend Checkout)
**Agente:** @dev
**Depende de:** Story 12.1 (Stripe Foundation)
**Complexity:** HIGH (7-8 story points)

---

## Story

**Como** hóspede,
**Quero** poder pagar por uma reserva direta em EUR (Stripe Portugal),
**Para que** o proprietário receba um pagamento automático via SEPA/IBAN e Lodgra receba a comissão.

---

## Contexto de Negócio

Lodgra oferece direct bookings (alternativa ao Airbnb). Quando hóspede faz reserva:
1. Cria payment intent em EUR (Stripe Portugal)
2. Split automático: proprietário recebe 85%, Lodgra 15%
3. Proprietário recebe via SEPA em 2-3 dias úteis
4. Suporta reembolsos (completo e parcial)

Usa Stripe Connect (Express account) para cada proprietário isolado (multi-tenant).

### O que já existe
- Stripe customer criado na signup
- Tabela organizations, bookings
- Auth para proprietários e hóspedes

### O que falta
- Onboarding para Connect (Express account por proprietário)
- Payment intent creation com split automático
- Webhook listeners para charge events
- Payout tracking e status
- Refund flow (UI + API)
- Checkout UI integrada

---

## Critérios de Aceitação

- [x] **AC1** — Proprietário pode fazer onboarding para Stripe Connect (Express account link)
- [x] **AC2** — Onboarding link gerado e armazenado em organizations.stripe_pt_connect_id
- [x] **AC3** — Hóspede pode criar payment intent para booking (EUR, com split 85/15)
- [x] **AC4** — Payment intent inclui proprietário como connected account (Stripe Connect)
- [x] **AC5** — Webhook processa: charge.succeeded, charge.failed, charge.refunded
- [x] **AC6** — Payment marcado como succeeded quando charge.succeeded recebido
- [x] **AC7** — Payout automático para proprietário via SEPA dentro de 2-3 dias úteis
- [x] **AC8** — Refund flow: hóspede/admin consegue refundar (completo ou parcial)
- [x] **AC9** — Checkout UI mostra: valor total + comissão Lodgra + valor para proprietário
- [x] **AC10** — Payment confirmation email enviado para ambos (hóspede + proprietário)
- [x] **AC11** — Refund confirmation email enviado quando refund processado
- [x] **AC12** — 10+ testes unitários (payment intent, split logic, refund flow)

---

## Riscos & Mitigação

**Payout Failures & Split Math Errors** — Stripe Connect split (85% owner, 15% Lodgra) pode falhar se destination account suspended ou se split percentages arredondadas incorrectamente (R$100 split em centavos causa erro). **Mitigation:** Testar split logic com 50+ valores (centavos, moedas fracionadas); implementar audit trail de todos os splits em database; monitorar failed payouts em Sentry com alertas imediatos; usar Stripe payout simulation em test mode.

**Fraud Detection & Chargeback Handling** — Hóspedes podem fazer pagamento com cartão roubado, chargebacks podem chegar meses depois. Se refund parcial falha, proprietário fica com saldo incorrecta. **Mitigation:** Implementar Stripe radar (fraud detection automática); documentar fluxo de chargeback (refund automático, email notificações); unit tests para refund partial logic com edge cases (refund > original amount, negative refund, etc.).

**Stripe Connect Account Limits & Onboarding** — Proprietários podem atingir limites Stripe (transaction volume, payout frequency), onboarding pode falhar se IBAN inválido. **Mitigation:** Validar IBAN format client-side antes de onboarding; documentar Stripe Connect límites em onboarding page; implementar webhook para account.updated (captura suspensões); graceful degradation se Connect account não activo.

---

## Tasks

- [ ] 1. Implementar Connect onboarding flow — GET /api/stripe/connect/link
- [ ] 2. Implementar Connect callback — POST /api/stripe/connect/callback
- [ ] 3. Criar payment intent endpoint — POST /api/stripe/payment-intent
- [ ] 4. Implementar split logic (destination: connected account, on_behalf_of)
- [ ] 5. Atualizar webhook /api/stripe/webhooks/booking — charge.succeeded/failed/refunded
- [ ] 6. Implementar refund endpoint — POST /api/billing/refunds
- [ ] 7. Criar componente checkout <CheckoutForm /> (Stripe Elements)
- [ ] 8. Adicionar checkout UI a página /[locale]/booking/[bookingId]/payment
- [ ] 9. Implementar email alerts (payment confirmed, refund processed)
- [ ] 10. Escrever 10+ testes unitários (payment intent, split, refund)
- [ ] 11. Verificar linting e type checking

---

## Dev Notes

### Connect Onboarding Flow

```typescript
// GET /api/stripe/connect/link
// Generates Express account link (redirect_url)
const accountLink = await stripePT.accountLinks.create({
  account: org.stripe_pt_connect_id || (new Express account),
  type: 'account_onboarding',
  refresh_url: `${baseUrl}/api/stripe/connect/refresh`,
  return_url: `${baseUrl}/dashboard/stripe-connect-success`,
});
```

### Payment Intent with Split

```typescript
// POST /api/booking/{bookingId}/payment
const paymentIntent = await stripePT.paymentIntents.create({
  amount: totalAmount * 100, // EUR cents
  currency: 'eur',
  on_behalf_of: organization.stripe_pt_connect_id,
  application_fee_amount: commission * 100, // Lodgra fee
  transfer_data: {
    destination: organization.stripe_pt_connect_id
  },
  metadata: {
    booking_id: bookingId,
    org_id: organization.id,
  }
});
```

### Webhook Events to Handle

```
charge.succeeded → payment.status = 'succeeded'
charge.failed → payment.status = 'failed'
charge.refunded → payment.status = 'refunded', create refund record
charge.dispute.created → payment.status = 'disputed', alert admin
```

### Refund Logic

```typescript
// Refund: full or partial
const refund = await stripePT.refunds.create({
  charge: payment.stripe_payment_id,
  amount: refundAmount * 100, // optional for partial
  reason: 'requested_by_customer' | 'duplicate' | 'fraudulent'
});

// Update payment record
await updatePayment(paymentId, {
  status: 'refunded',
  refund_amount: refundAmount,
  refund_reason: reason,
});
```

---

## Testing Strategy

**Unit Tests (10+):**
- Connect onboarding: gera link redirect válida, armazena connect_id
- Payment intent: calcula split 85/15 correctamente (rounding tested)
- Refund logic: full refund, partial refund, invalid refund (amount > charged)
- Split calculator: edge cases (R$1 = 1 centavo split, R$0.01 rounding)

**Integration Tests (Stripe Test Mode):**
- Create Connect account: Express account link gerado, owner pode completar
- Create payment intent: Stripe retorna payment_intent_id, client_secret
- Charge succeeded: webhook processa charge.succeeded, payout_to_owner criado
- Refund charge: webhook processa charge.refunded, refund_amount logged

**Manual Testing (Stripe Dashboard + Connect Onboarding):**
- Stripe Portugal dashboard: verificar Connect Express account criado
- Connect onboarding: fazer onboarding com test IBAN (DE89370400440532013000)
- Checkout UI: mostra split breakdown (guest pays X, Lodgra fee Y, owner receives Z)
- Payout tracking: verificar payout pendente/concluído em Stripe dashboard

**⚠️ Nota:** Todos em **Stripe test mode**. Connect account pode ser testado com Stripe test IBAN (não precisa IBAN real). Produção: testar com real proprietários + real IBAN antes de lanço.

---

## File List

- `src/app/api/stripe/connect/link/route.ts` — NEW: Generate Express account link
- `src/app/api/stripe/connect/callback/route.ts` — NEW: Handle onboarding callback
- `src/app/api/stripe/payment-intent/route.ts` — NEW: Create payment intent for booking
- `src/app/api/billing/refunds/route.ts` — NEW: Create refund
- `src/lib/stripe/webhooks/charge.ts` — NEW: Handle charge events
- `src/lib/stripe/split-calculator.ts` — NEW: Calculate split (commission + owner payout)
- `src/components/payment/CheckoutForm.tsx` — NEW: Stripe Elements checkout
- `src/app/[locale]/booking/[bookingId]/payment/page.tsx` — NEW: Payment page with checkout
- `src/app/[locale]/dashboard/stripe-connect-success/page.tsx` — NEW: Onboarding success page
- `src/lib/emails/payment-confirmed.ts` — NEW: Email template for payment confirmation
- `src/lib/emails/refund-processed.ts` — NEW: Email template for refund notification
- `src/app/api/stripe/webhooks/booking/__tests__/charge.test.ts` — NEW: 10+ tests
- `.env.production` — MODIFIED: STRIPE_PT_WEBHOOK_SECRET

---

## Dev Agent Record

### Agent Model Used
Claude Haiku 4.5

### Completion Notes
- [ ] All 11 tasks completed
- [ ] 10+ tests passing
- [ ] Linting: PASS
- [ ] Type checking: PASS
- [ ] Manual booking payment test in Stripe test mode verified

### Change Log
- 2026-05-16 16:45 UTC: Validated GO (10/10) — Status: Draft → Ready. All criteria met (title, AC, scope, dependencies, complexity, value, risks, DoD, alignment). Ready for @dev implementation. | @po
- [Date]: Story created (Draft)
