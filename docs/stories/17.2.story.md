# Story 17.2: Propriedades — documentos, vídeos e remoção campo URL fotos

**Epic:** 17 — Anexos de Documentos
**Status:** Ready for Dev
**Prioridade:** P1 — Depende de 17.1 (reutilizar FileUpload)
**Tipo:** Full Stack
**Agente:** @dev

---

## Story

**Como** gestor de propriedades,
**Quero** anexar contratos, comprovantes e vídeos de instrução a uma propriedade,
**Para que** toda a documentação relevante esteja centralizada no perfil da propriedade.

---

## Acceptance Criteria

- [ ] **AC1** — Upload de documentos: PDF, .doc/.docx (máx 20MB)
- [ ] **AC2** — Upload de vídeos: mp4, mov (máx 100MB)
- [ ] **AC3** — Ficheiros listados no perfil da propriedade com nome, tipo e data
- [ ] **AC4** — Download/visualização disponível
- [ ] **AC5** — Remoção individual com confirmação
- [ ] **AC6** — Storage path: `properties/{property_id}/documents/{uuid}-{filename}`
- [ ] **AC7** — Campo "URL de fotos" removido do formulário de edição de propriedade
- [ ] **AC8** — Remoção do campo URL fotos não afecta upload de fotos existente

---

## Dev Notes

### Storage path

```
properties/{property_id}/documents/{uuid}-{original_filename}
```

### Nova tabela necessária

```sql
CREATE TABLE property_documents (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  property_id uuid NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
  file_name text NOT NULL,
  file_path text NOT NULL,
  file_size integer,
  mime_type text,
  document_type text, -- 'contract', 'proof', 'video', 'other'
  created_at timestamptz DEFAULT now()
);
```

### Campo a remover

Identificar e remover o campo de URL de fotos no formulário de edição de propriedade (`src/app/[locale]/properties/[id]/edit/page.tsx` ou equivalente). Confirmar que a coluna da BD pode ser deixada (não remover da BD para não perder dados existentes, apenas esconder da UI).

### Tipos MIME permitidos

```typescript
const ALLOWED_DOC_TYPES = ['application/pdf', 'application/msword', 'application/vnd.openxmlformats-officedocument.wordprocessingml.document']
const ALLOWED_VIDEO_TYPES = ['video/mp4', 'video/quicktime']
```

---

## Tasks

- [ ] Criar migração SQL: tabela `property_documents`
- [ ] Reutilizar `FileUpload.tsx` de Story 17.1
- [ ] Integrar upload no formulário de edição de propriedade
- [ ] Suporte a vídeos (tipos MIME + limite 100MB)
- [ ] Listar documentos no perfil da propriedade
- [ ] Implementar remoção com confirmação
- [ ] Remover campo "URL de fotos" do formulário
- [ ] Testes de upload e remoção

---

## Dev Agent Record

### Agent Model Used
### Completion Notes
### File List
### Change Log
