# Story 17.2: Propriedades — documentos, vídeos e remoção campo URL fotos

**Epic:** 17 — Anexos de Documentos
**Status:** Ready for Review
**Prioridade:** P1 — Depende de 17.1 (reutilizar FileUpload)
**Tipo:** Full Stack
**Agente:** @dev

---

## Story

**Como** gestor de propriedades,
**Quero** anexar contratos, comprovantes e vídeos de instrução a uma propriedade,
**Para que** toda a documentação relevante esteja centralizada no perfil da propriedade.

---

## Acceptance Criteria

- [x] **AC1** — Upload de documentos: PDF, .doc/.docx (máx 20MB)
- [x] **AC2** — Upload de vídeos: mp4, mov (máx 100MB)
- [x] **AC3** — Ficheiros listados no perfil da propriedade com nome, tipo e data
- [x] **AC4** — Download/visualização disponível
- [x] **AC5** — Remoção individual com confirmação
- [x] **AC6** — Storage path: `properties/{property_id}/documents/{uuid}-{filename}`
- [x] **AC7** — Campo "URL de fotos" removido do formulário de edição de propriedade
- [x] **AC8** — Remoção do campo URL fotos não afecta upload de fotos existente

---

## Dev Notes

### Storage path

```
properties/{property_id}/documents/{uuid}-{original_filename}
```

### Nova tabela necessária

```sql
CREATE TABLE property_documents (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  property_id uuid NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
  file_name text NOT NULL,
  file_path text NOT NULL,
  file_size integer,
  mime_type text,
  document_type text, -- 'contract', 'proof', 'video', 'other'
  created_at timestamptz DEFAULT now()
);
```

### Campo a remover

Identificar e remover o campo de URL de fotos no formulário de edição de propriedade (`src/app/[locale]/properties/[id]/edit/page.tsx` ou equivalente). Confirmar que a coluna da BD pode ser deixada (não remover da BD para não perder dados existentes, apenas esconder da UI).

### Tipos MIME permitidos

```typescript
const ALLOWED_DOC_TYPES = ['application/pdf', 'application/msword', 'application/vnd.openxmlformats-officedocument.wordprocessingml.document']
const ALLOWED_VIDEO_TYPES = ['video/mp4', 'video/quicktime']
```

---

## Tasks

- [x] Criar migração SQL: tabela `property_documents`
- [x] Reutilizar `FileUpload.tsx` de Story 17.1
- [x] Integrar upload no formulário de edição de propriedade
- [x] Suporte a vídeos (tipos MIME + limite 100MB)
- [x] Listar documentos no perfil da propriedade
- [x] Implementar remoção com confirmação
- [x] Remover campo "URL de fotos" do formulário
- [x] Testes de upload e remoção

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- Migração `20260504_02_property_documents.sql`: tabela `property_documents` + bucket `property-documents` + RLS (org isolation directo via properties.organization_id)
- `FileUpload.tsx` actualizado: props `allowedTypes`, `maxFileSize`, `maxFiles`, `acceptAttr`, `hint` para reutilização configurável
- `PropertyDocuments.tsx`: dois upload zones (docs 20MB + vídeos 100MB), lista unificada com ícone de vídeo/doc e data
- API routes: GET/POST `/api/properties/[id]/documents`, GET (signed URL) + DELETE `/[docId]`
- Campo "URLs das Fotos" removido do edit form: estado `photos`, loadProperty, handleSubmit e JSX textarea — coluna DB preservada (AC8)
- Documentos adicionados ao perfil da propriedade após o iCalExportCard (col-span-2 main column)

### File List
- `supabase/migrations/20260504_02_property_documents.sql` (new)
- `src/types/database.ts` (modified — PropertyDocument type)
- `src/components/common/ui/FileUpload.tsx` (modified — configurable props)
- `src/components/features/properties/PropertyDocuments.tsx` (new)
- `src/app/api/properties/[id]/documents/route.ts` (new)
- `src/app/api/properties/[id]/documents/[docId]/route.ts` (new)
- `src/app/[locale]/properties/[id]/page.tsx` (modified)
- `src/app/[locale]/properties/[id]/edit/page.tsx` (modified — removed photos URL field)

### Change Log
- 2026-05-04: Story 17.2 completa (Dex, claude-sonnet-4-6)


---

## QA Results

### Review Date: 2026-05-06
### Reviewed By: Quinn (Test Architect)
### Code Quality Assessment
Padrão idêntico ao 17.1. Limite diferenciado 20MB/100MB para docs/vídeos, FileUpload.tsx com props configuráveis, campo URL fotos completamente removido do edit form sem referências residuais.
### Refactoring Performed
Nenhum. Zero correcções necessárias.
### Compliance Check
- All ACs Met: ✓ AC1–AC8
### Gate Status
Gate: PASS (100/100) → docs/qa/gates/17.2-property-documents.yml
### Recommended Status
[✓ Ready for Done]
