# Story 14.2: Property Landing Page V2 — Redesign Holidu-like

**Epic:** 14 — Property Public Experience
**Status:** ✅ Done
**Prioridade:** P1 — Core do epic, impacto directo na conversão
**Tipo:** Frontend (redesign completo)
**Agente:** @dev
**Depende de:** Story 14.1 (Design System ✅)

---

## Story

**Como** visitante de uma página pública de propriedade (`/p/[slug]`),
**Quero** uma experiência visual moderna e imersiva (galeria full-screen, booking widget claro, mapa e comodidades detalhadas),
**Para que** me sinta confiante para reservar directamente sem recorrer a uma OTA.

---

## Contexto de Negócio

A página `/p/[slug]` actual é funcional mas minimalista. Plataformas líderes como Holidu e Airbnb demonstram que a experiência visual do imóvel é o principal driver de conversão. O redesign deve:

1. **Galeria imersiva** — hero full-width com lightbox, grid Masonry ou layout Airbnb
2. **Booking widget sticky** — preço, calendário inline, CTA no desktop; bottom sheet no mobile
3. **Conteúdo estruturado** — descrição expandível, comodidades por categoria, localização
4. **SEO** — estrutura semântica, JSON-LD já implementado (manter)
5. **Performance** — imagens optimizadas (já usa `property_images` + `image_variants`), lazy loading

### Referências de Design

- **Holidu:** layout 2 colunas (conteúdo | booking), galeria grid 1+4, badge "Reserva directa"
- **Airbnb:** hero panorâmico, floating booking card, estrelas/reviews
- **Identity Home Stay:** tons brand (azul-petróleo), accent coral, tipografia Inter

---

## Critérios de Aceitação

### Galeria
- [x] **AC1** — Hero com grid de fotos: 1 foto grande à esquerda + 4 thumbnails à direita (desktop). Em mobile: carousel swipeable
- [x] **AC2** — Botão "Ver todas as fotos" abre lightbox full-screen com navegação por teclado e swipe (mobile)
- [x] **AC3** — Imagens usam `image_variants` (WebP, sizes optimizados) quando disponíveis; fallback para `photos[]`

### Header e Título
- [x] **AC4** — Header sticky scroll-aware: transparent → sólido (bg-white + shadow) ao scroll > 80px. Logo e badge adaptam cor.
- [x] **AC5** — Título da propriedade, localização com ícone, e quick stats (hóspedes, quartos, casas de banho) com ícones Lucide

### Booking Widget
- [x] **AC6** — Desktop: card booking sticky no lado direito com preço/noite, seletor de datas, CTA "Reservar agora" em lodgra-green (#059669)
- [x] **AC7** — Mobile: bottom bar fixa com preço + CTA; ao clicar, abre sheet/modal com calendário
- [x] **AC8** — Preço base visível. "Preço base: X/noite" com currency symbol dinâmico

### Conteúdo
- [x] **AC9** — Secção "Sobre este espaço": texto expandível (collapsed 120px, botão "Ver mais")
- [x] **AC10** — Secção "Comodidades": agrupadas por categoria (Essenciais, Cozinha, Exterior, Extra) com ícones Lucide. Modal "Ver todas" se > 8
- [x] **AC11** — Secção "Localização": embed Google Maps sem API key (`maps.google.com/maps?q=...&output=embed`). Fallback gracioso se sem endereço

### Footer da Página
- [x] **AC12** — Footer: "© Lodgra · Reservas directas sem comissões" + links Política de Privacidade e Termos
- [x] **AC13** — Trust badges: "Pagamento seguro · Sem comissões · Suporte 24h"

### Performance & SEO
- [x] **AC14** — Imagens above-the-fold com `priority` prop (hero image). LCP otimizado
- [x] **AC15** — Estrutura HTML semântica: `<header>`, `<main>`, `<section>`, `<footer>` em uso
- [x] **AC16** — JSON-LD e `generateMetadata` mantidos em `src/app/p/[slug]/page.tsx`

### Responsive
- [x] **AC17** — Layout 2 colunas (`lg:col-span-2` + `lg:col-span-1`) no desktop
- [x] **AC18** — Mobile: coluna única, galeria carousel, bottom booking bar fixa

---

## Dev Notes

### Arquitectura de Componentes

```
src/app/p/[slug]/page.tsx             ← server component (dados)
src/components/public/
  PropertyPageV2.tsx                  ← client wrapper principal
  gallery/
    PropertyHeroGallery.tsx           ← grid hero + lightbox trigger
    PropertyLightbox.tsx              ← lightbox full-screen (client)
  booking/
    BookingWidgetDesktop.tsx          ← card sticky (desktop)
    BookingWidgetMobile.tsx           ← bottom bar + sheet (mobile)
  content/
    PropertyDescription.tsx          ← texto expandível
    PropertyAmenitiesV2.tsx          ← amenities por categoria com ícones
    PropertyLocation.tsx             ← mapa estático
  layout/
    PropertyPageHeader.tsx           ← header sticky com scroll behaviour
    PropertyTrustBadges.tsx          ← badges de confiança
```

### Mapeamento de Comodidades por Categoria

```ts
const AMENITY_CATEGORIES: Record<string, { icon: LucideIcon; label: string }> = {
  // Essenciais
  wifi: { icon: Wifi, label: 'WiFi' },
  air_conditioning: { icon: Wind, label: 'Ar condicionado' },
  heating: { icon: Thermometer, label: 'Aquecimento' },
  // Cozinha
  kitchen: { icon: ChefHat, label: 'Cozinha equipada' },
  dishwasher: { icon: Waves, label: 'Máquina de lavar louça' },
  // Exterior
  pool: { icon: Waves, label: 'Piscina' },
  parking: { icon: Car, label: 'Estacionamento' },
  // ...
}
```

### Booking Widget — interacção com AvailabilityCalendar

O componente `AvailabilityCalendar` existente (`src/components/public/AvailabilityCalendar.tsx`) deve ser reutilizado/adaptado:
- Desktop: embutido no card booking (sem border extra)
- Mobile: dentro de um Sheet/Modal (usar Radix Dialog ou solução nativa)

### Mapa Estático

Usar OpenStreetMap Staticmap (gratuito, sem API key):
```
https://staticmap.openstreetmap.de/staticmap.php?center={lat},{lng}&zoom=14&size=600x300&markers={lat},{lng},lightblue
```
Se `property.latitude` e `property.longitude` existirem na tabela. Se não existirem, mostrar placeholder com endereço textual.

> Verificar se colunas `latitude`/`longitude` existem em `properties`. Se não, não bloquear — mostrar só endereço.

### Header Scroll Behaviour

```tsx
// PropertyPageHeader.tsx (client component)
const [scrolled, setScrolled] = useState(false)
useEffect(() => {
  const onScroll = () => setScrolled(window.scrollY > 80)
  window.addEventListener('scroll', onScroll, { passive: true })
  return () => window.removeEventListener('scroll', onScroll)
}, [])
// className: scrolled ? 'bg-white shadow-sm' : 'bg-transparent'
```

### Ficheiros a Criar/Modificar

```
src/app/p/[slug]/page.tsx                     ← modificar (usar PropertyPageV2)
src/components/public/PropertyPageV2.tsx      ← criar (wrapper)
src/components/public/gallery/
  PropertyHeroGallery.tsx                     ← criar
  PropertyLightbox.tsx                        ← criar
src/components/public/booking/
  BookingWidgetDesktop.tsx                    ← criar
  BookingWidgetMobile.tsx                     ← criar
src/components/public/content/
  PropertyDescription.tsx                    ← criar
  PropertyAmenitiesV2.tsx                    ← criar (substitui PropertyAmenities.tsx)
  PropertyLocation.tsx                       ← criar
src/components/public/layout/
  PropertyPageHeader.tsx                     ← criar
  PropertyTrustBadges.tsx                    ← criar
```

---

## Tasks

- [x] 1. `PropertyHeroGallery` — grid 1+4 desktop, carousel mobile (já existia, validado)
- [x] 2. `PropertyLightbox` — full-screen, teclado ←→/Esc, thumbnails (já existia, validado)
- [x] 3. `PropertyPageHeader` — sticky scroll-aware: transparent→solid ao scroll > 80px ✅ (corrigido)
- [x] 4. `BookingWidgetDesktop` — CTA em lodgra-green (#059669) ✅ (corrigido de orange)
- [x] 5. `BookingWidgetMobile` — CTA em lodgra-green, bottom sheet ✅ (corrigido de orange)
- [x] 6. `PropertyDescription` — texto expandível (já existia, validado)
- [x] 7. `PropertyAmenitiesV2` — categorias, ícones Lucide, modal (já existia, validado)
- [x] 8. `PropertyLocation` — mapa sem API key hardcoded ✅ (corrigido, usa embed free)
- [x] 9. `PropertyTrustBadges` — 3 badges de confiança (já existia, validado)
- [x] 10. `PropertyPageV2` — footer AC12 + quick stats com ícones Lucide ✅ (corrigido)
- [x] 11. `src/app/p/[slug]/page.tsx` — já usa `PropertyPageV2` (sem alteração necessária)
- [x] 12. Imagens com `priority` no hero, lazy no resto
- [x] 13. TypeScript 0 erros | Lint 0 erros nos ficheiros alterados

---

## File List

```
src/components/common/public/PropertyPageV2.tsx                     ← modificado: footer + quick stats ícones Lucide
src/components/common/public/layout/PropertyPageHeader.tsx          ← modificado: scroll-aware (transparent→solid)
src/components/common/public/booking/BookingWidgetDesktop.tsx       ← modificado: orange→lodgra-green CTAs
src/components/common/public/booking/BookingWidgetMobile.tsx        ← modificado: orange→lodgra-green CTAs
src/components/common/public/content/PropertyLocation.tsx           ← modificado: remove hardcoded Google API key
```

> Nota: Todos os componentes já existiam em `src/components/common/public/` (não em `src/components/public/` como indicado na story). Componentes `PropertyHeroGallery`, `PropertyLightbox`, `PropertyDescription`, `PropertyAmenitiesV2`, `PropertyTrustBadges` estavam completos e correctos.

## Dev Agent Record

### Completion Notes
- Componentes já implementados em ciclo anterior em `common/public/`, não `public/`
- Fixes aplicados: scroll-aware header, brand colors (lodgra-green), API key removida, footer adicionado
- Google Maps embed usa formato sem API key: `maps.google.com/maps?q=...&output=embed`
- CTAs migrados de `#FF6B00` (orange-500) para `#059669` (lodgra-green) — consistência com BrazilLanding
- TypeScript 0 erros | Lint 0 novos erros

### Agent Model Used
claude-sonnet-4-6
