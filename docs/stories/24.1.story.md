# Story 24.1: Schema.org VacationRental — Markup Completo para Google Vacation Rentals

**Epic:** 24 — Google Vacation Rentals Distribution
**Status:** Approved
**Prioridade:** P1 — Alta: pré-requisito para distribuição Google
**Tipo:** Frontend + SEO
**Agente:** @dev

---

## Story

**Como** proprietário que lista imóveis no Lodgra,
**Quero** que as páginas públicas dos imóveis tenham Schema.org `VacationRental` completo e válido,
**Para que** o Google possa indexar e distribuir os meus imóveis no Google Vacation Rentals sem comissões de OTA.

---

## Context

A função `generatePropertyJsonLd()` em `src/lib/seo/jsonld.ts` já existe e usa `@type: VacationRental` (subtipo correcto de `LodgingBusiness` para Google Vacation Rentals). No entanto, tem vários problemas que impedem a validação pelo Google:

1. **`APP_URL` errado** — aponta para `https://www.lodgra.pt` em vez de `https://lodgra.io`
2. **Nonce em falta** — `<script type="application/ld+json">` na página não tem o atributo `nonce`, causando potencial violação CSP
3. **Imagens desactualizadas** — o JSON-LD usa apenas `property.photos[0]` (array legado) em vez das imagens WebP do sistema `property_images`
4. **Amenities em falta** — as amenities estruturadas são carregadas na página mas nunca passadas ao `generatePropertyJsonLd()`
5. **Campos obrigatórios Google em falta** — `@id`, `url`, `numberOfBathroomsTotal`, `occupancy`, `petsAllowed`

**Decisão de tipo Schema.org:**
`VacationRental` (não `LodgingBusiness`) — é o tipo correcto e reconhecido pelo Google Vacation Rentals para propriedades de alojamento temporário de uso privado.

**O que já funciona:**
- Página pública: `/p/[slug]` com URL única ✅
- JSON-LD é renderizado na página (`<script type="application/ld+json">`) ✅
- OpenGraph metadata completo ✅
- Dados carregados: preço base, taxas, check-in/out, quartos, casas de banho ✅

---

## Acceptance Criteria

- **AC1** — `APP_URL` em `src/lib/seo/jsonld.ts` usa `process.env.NEXT_PUBLIC_APP_URL` com fallback para `https://lodgra.io` (não `www.lodgra.pt`)
- **AC2** — O `<script type="application/ld+json">` na página `/p/[slug]` inclui o atributo `nonce` lido de `headers()`
- **AC3** — O JSON-LD inclui pelo menos uma imagem WebP do sistema `property_images` (quando disponível); fallback para `photos[]` legado
- **AC4** — O JSON-LD inclui `amenityFeature` com as amenities estruturadas carregadas da tabela `property_amenities`
- **AC5** — O JSON-LD inclui `@id`, `url`, `numberOfRooms`, `numberOfBathroomsTotal`, `occupancy.maxValue`
- **AC6** — Validação positiva no [Google Rich Results Test](https://search.google.com/test/rich-results) para pelo menos uma propriedade de teste
- **AC7** — Sem erros de CSP nos browser DevTools após o deploy (o nonce elimina o risco)
- **AC8** — `generateWebsiteJsonLd()` é adicionado ao layout raiz (`src/app/layout.tsx`) para o Website schema

---

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- **Primary Type**: Frontend / SEO
- **Secondary Type(s)**: Integration (Google Schema.org)
- **Complexity**: Low-Medium

**Specialized Agent Assignment:**
- **Primary**: @dev — implementação e pre-commit review
- **Supporting**: @github-devops — PR e deploy para validação em produção

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): Verificar JSON-LD output com `JSON.stringify` e validar schema manualmente
- [ ] Pre-PR (@github-devops): Criar PR para merge em main
- [ ] Pre-Deployment: Validar no Google Rich Results Test após deploy em produção

**Self-Healing Configuration:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL only

**Predicted Behavior:**
- CRITICAL issues: auto_fix (2 iterações)
- HIGH issues: document_only em Dev Notes

**CodeRabbit Focus Areas:**
- **Primary**: Sanitização do JSON-LD output (sem XSS via `dangerouslySetInnerHTML`)
- **Primary**: Nonce propagation correcto (sem nonce hardcoded)
- **Secondary**: Fallback gracioso quando `property_images` está vazio
- **Secondary**: Tipos TypeScript correctos na interface `PropertyData`

---

## Tasks / Subtasks

- [ ] **Task 1** — Corrigir `APP_URL` em `src/lib/seo/jsonld.ts` (AC1)
  - [ ] 1.1 — Substituir `'https://www.lodgra.pt'` por `process.env.NEXT_PUBLIC_APP_URL || 'https://lodgra.io'`
  - [ ] 1.2 — Verificar que `generateWebsiteJsonLd()` também usa a variável correcta

- [ ] **Task 2** — Adicionar nonce ao script JSON-LD na página `/p/[slug]` (AC2)
  - [ ] 2.1 — Importar `headers` de `next/headers` na página
  - [ ] 2.2 — Ler `x-nonce` do headers: `const nonce = (await headers()).get('x-nonce') ?? undefined`
  - [ ] 2.3 — Passar `nonce={nonce}` ao elemento `<script>` do JSON-LD

- [ ] **Task 3** — Passar imagens WebP ao `generatePropertyJsonLd()` (AC3)
  - [ ] 3.1 — Actualizar interface `PropertyData` em `jsonld.ts` para aceitar `imageUrls?: string[]`
  - [ ] 3.2 — Actualizar `generatePropertyJsonLd()` para usar `imageUrls` em vez de `photos`; se `imageUrls` tem mais de 1 elemento, usar array; caso contrário string
  - [ ] 3.3 — Na página, passar `allPhotos` (já calculado, inclui WebP) como `imageUrls` ao chamar `generatePropertyJsonLd()`

- [ ] **Task 4** — Adicionar amenities estruturadas ao JSON-LD (AC4)
  - [ ] 4.1 — Actualizar interface `PropertyData` para aceitar `structuredAmenities?: Array<{name: string; icon?: string; category?: string}>`
  - [ ] 4.2 — Actualizar `generatePropertyJsonLd()`: fazer merge das amenities de capacidade existentes com as amenities do catálogo em `amenityFeature`
  - [ ] 4.3 — Na página, passar `structuredAmenities` (já carregado) ao chamar `generatePropertyJsonLd()`

- [ ] **Task 5** — Adicionar campos obrigatórios Google ao JSON-LD (AC5)
  - [ ] 5.1 — Adicionar `@id: ${APP_URL}/p/${property.slug}` (para identificação única da entidade)
  - [ ] 5.2 — Adicionar `numberOfBathroomsTotal: property.bathrooms` (quando disponível)
  - [ ] 5.3 — Adicionar `occupancy: { '@type': 'QuantitativeValue', maxValue: property.max_guests }` (quando disponível)
  - [ ] 5.4 — Adicionar `postal_code` a `PropertyData` e ao `PostalAddress.postalCode` (a página já faz select de `postal_code`)
  - [ ] 5.5 — Verificar tipos TypeScript e remover `as const` desnecessários

- [ ] **Task 6** — Adicionar `WebSite` JSON-LD ao layout raiz (AC8)
  - [ ] 6.1 — Em `src/app/layout.tsx`, chamar `generateWebsiteJsonLd()` e renderizar `<script type="application/ld+json">` com nonce (já disponível no layout)

- [ ] **Task 7** — Validação (AC6, AC7)
  - [ ] 7.1 — Após deploy, testar uma propriedade no Google Rich Results Test e confirmar `VacationRental` detectado sem erros
  - [ ] 7.2 — Confirmar ausência de erros CSP nos DevTools (Network + Console)
  - [ ] 7.3 — Confirmar `image`, `address`, `checkinTime`, `checkoutTime`, `makesOffer`, `amenityFeature` presentes no JSON output

---

## Dev Notes

### Ficheiros-alvo

| Ficheiro | Alteração |
|---|---|
| `src/lib/seo/jsonld.ts` | Fix APP_URL; expandir interface; novos campos |
| `src/app/p/[slug]/page.tsx` | Adicionar nonce ao script; passar `allPhotos`, `structuredAmenities` |
| `src/app/layout.tsx` | Adicionar WebSite JSON-LD com nonce |

### Padrão de nonce na página (para replicar em `/p/[slug]/page.tsx`)

O layout raiz já usa este padrão (linha 82 de `src/app/layout.tsx`):
```typescript
import { headers } from 'next/headers'
const nonce = (await headers()).get('x-nonce') ?? undefined
```

O nonce vem do middleware (`src/middleware.ts` linha 25) — gerado por request como `Buffer.from(crypto.randomUUID()).toString('base64')` e propagado via header `x-nonce`.

### Padrão de script JSON-LD com nonce

```tsx
<script
  type="application/ld+json"
  nonce={nonce}
  dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
/>
```

### Dados disponíveis na página `/p/[slug]`

Já carregados e disponíveis para passar ao `generatePropertyJsonLd()`:
- `allPhotos: string[]` — URLs WebP das `property_images`, fallback para `property.photos[]`
- `structuredAmenities: AmenityRow[]` — amenities do catálogo com `name`, `icon`, `category`
- `property.postal_code` — já no select da query (linha 74)
- `property.bathrooms` — já no select
- `property.max_guests` — já no select

### Schema.org VacationRental — campos mínimos Google

```json
{
  "@context": "https://schema.org",
  "@type": "VacationRental",
  "@id": "https://lodgra.io/p/slug",
  "name": "Nome do imóvel",
  "description": "...",
  "image": ["url1.webp", "url2.webp"],
  "url": "https://lodgra.io/p/slug",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "...",
    "addressLocality": "Lisboa",
    "postalCode": "1000-001",
    "addressCountry": "PT"
  },
  "checkinTime": "15:00",
  "checkoutTime": "11:00",
  "occupancy": { "@type": "QuantitativeValue", "maxValue": 6 },
  "numberOfRooms": 2,
  "numberOfBathroomsTotal": 1,
  "amenityFeature": [
    { "@type": "LocationFeatureSpecification", "name": "WiFi", "value": true }
  ],
  "makesOffer": [{
    "@type": "Offer",
    "price": 120,
    "priceCurrency": "EUR"
  }]
}
```

### CSP e nonce

O middleware (`src/middleware.ts:25`) gera um nonce por request. O `applySecurityHeaders()` em `src/lib/middleware/security-headers.ts` aplica `script-src 'nonce-{nonce}'`. Qualquer `<script>` inline sem esse nonce será bloqueado. O layout raiz já propaga o nonce correctamente — replicar o mesmo padrão na página `/p/[slug]`.

### Testing

- Não são necessários testes unitários para este story (JSON-LD é geração de dados estáticos)
- Testar manualmente com o Google Rich Results Test após deploy
- Verificar output com `console.log(JSON.stringify(jsonLd, null, 2))` em dev se necessário
- Linting: `npm run lint` deve passar sem erros TypeScript

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-10 | 1.0 | Story criada — Schema.org VacationRental para Google Vacation Rentals | River (@sm) |

---

## Dev Agent Record

### Agent Model Used

_A preencher pelo agente de desenvolvimento_

### Debug Log References

_A preencher pelo agente de desenvolvimento_

### Completion Notes List

_A preencher pelo agente de desenvolvimento_

### File List

_A preencher pelo agente de desenvolvimento_
