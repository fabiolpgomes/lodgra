# Story 20.3: Google Feed — Correcção Query de Disponibilidade

**Epic:** 20 — Checkout Directo (Housekeeping)
**Status:** In Progress
**Prioridade:** P0 — Bug crítico: disponibilidade no feed usa colunas erradas (igual ao bug 20.1)
**Tipo:** Bugfix
**Agente:** @dev
**Depende de:** Story 20.1 ✅

---

## Context

`/api/google-feed/route.ts` tem o mesmo bug de query que Story 20.1 corrigiu em `page.tsx`:
- Usa `property_id` na tabela `reservations` (coluna não existe)
- Usa `start_date`/`end_date` (colunas não existem — correcto: `check_in`/`check_out`)
- Omite status `pending_payment` (reservas directas Stripe ficam invisíveis)

Resultado: `blocked_ranges` no feed está sempre vazio — Google vê todas as datas como disponíveis.

---

## Acceptance Criteria

- [x] **AC1** — Query de reservas usa two-step: `property_listings` → `property_listing_id IN (...)`
- [x] **AC2** — Colunas correctas: `check_in`, `check_out` (não `start_date`/`end_date`)
- [x] **AC3** — Status inclui `['confirmed', 'pending_payment', 'pending']`
- [x] **AC4** — Guard para `listingIds` vazio (evita `IN ()`)
- [x] **AC5** — `blocked_ranges` indexado correctamente por `property_id` via mapa inverso
- [x] **AC6** — Build PASS + Lint PASS

---

## Dev Notes

### Fix — Two-step availability query

```ts
// Step 1: fetch all listing IDs for public properties
const { data: listingsData } = await adminClient
  .from('property_listings')
  .select('id, property_id')
  .in('property_id', propertyIds)

// Build reverse map: listing_id → property_id
const listingToProperty: Record<string, string> = {}
const allListingIds: string[] = []
for (const l of listingsData ?? []) {
  listingToProperty[l.id] = l.property_id
  allListingIds.push(l.id)
}

// Step 2: query reservations via listing IDs
const blockedByProperty: Record<string, { start: string; end: string }[]> = {}
if (allListingIds.length > 0) {
  const { data: reservations } = await adminClient
    .from('reservations')
    .select('property_listing_id, check_in, check_out')
    .in('property_listing_id', allListingIds)
    .in('status', ['confirmed', 'pending_payment', 'pending'])
    .gte('check_out', today)
    .lte('check_in', futureDate)
    .order('check_in', { ascending: true })

  for (const r of reservations ?? []) {
    const propId = listingToProperty[r.property_listing_id]
    if (!propId) continue
    if (!blockedByProperty[propId]) blockedByProperty[propId] = []
    blockedByProperty[propId].push({ start: r.check_in, end: r.check_out })
  }
}
```

---

## Tasks

- [x] 1. Fix availability query em google-feed/route.ts (AC1–AC5)
- [x] 2. Build PASS + Lint PASS (AC6)

---

## File List

```
src/app/api/google-feed/route.ts  ← modified
docs/stories/20.3.story.md        ← created
```

## Dev Agent Record

### Status: Done
### Agent Model Used: claude-sonnet-4-6

### Completion Notes
- Two-step pattern idêntico ao fix de Story 20.1 mas para o feed em batch
- Mapa inverso `listingToProperty` resolve listing_id → property_id sem N+1 queries
- Guard `if (allListingIds.length > 0)` previne `IN ()` vazio

### Change Log
- 2026-05-07: Story 20.3 — google feed availability fix (Dex, claude-sonnet-4-6)

---

## QA Results

### Review Date: 2026-05-07
### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Fix correcto e completo. Two-step idêntico ao padrão validado em 20.1. Mapa inverso `listingToProperty` é O(n) e sem N+1. Guard `if (allListingIds.length > 0)` presente. Status alargado a `pending_payment`. Colunas correctas `check_in`/`check_out`. Sem nova dependência. 0 erros TypeScript, 0 erros ESLint.

### Compliance Check
- AC1–AC6: ✓ todos
- Two-step query: ✓ guard para listingIds vazio
- Reverse map: ✓ listing_id → property_id sem N+1

### Gate Status
Gate: PASS (100/100) → docs/qa/gates/20.3-google-feed-availability-fix.yml

### Recommended Status
[✓ Done]
