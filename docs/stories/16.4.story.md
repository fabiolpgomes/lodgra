# Story 16.4: Export PDF listagem — colunas adultos, crianças e notas

**Epic:** 16 — Reservas: Dados de Hóspedes e PDF
**Status:** Ready for Review
**Prioridade:** P3 — Depende de 16.1
**Tipo:** Frontend / Export
**Agente:** @dev

---

## Story

**Como** gestor de propriedades,
**Quero** que o PDF exportado da listagem de reservas inclua adultos, crianças e notas,
**Para que** o relatório seja completo para análise e partilha.

---

## Acceptance Criteria

- [x] **AC1** — Coluna "Adultos" adicionada ao PDF de listagem
- [x] **AC2** — Coluna "Crianças" adicionada ao PDF de listagem
- [x] **AC3** — Coluna "Notas" adicionada ao PDF de listagem (truncar se longa)
- [x] **AC4** — Layout da tabela continua legível (ajustar larguras se necessário)

---

## Dev Notes

### Ficheiro relevante

- `src/components/features/reports/ReservationsPdfGenerator.tsx`
- `src/app/[locale]/reservations/export/page.tsx`

Verificar se a query de export já inclui `adults`, `children` e `notes`. Se não, actualizar o select.

### Nota sobre largura

Com mais colunas, considerar orientação landscape no PDF ou reduzir font-size da tabela.

---

## Tasks

- [x] Actualizar query de export para incluir `adults`, `children`, `notes`
- [x] Adicionar colunas ao template PDF
- [x] Ajustar layout para acomodar novas colunas
- [x] Testar geração do PDF

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- Consulta das duas rotas migrada de `status`, `total_amount`, `property_listings` e `guests` para os campos canônicos Multi-OTA (`reservation_status`, `total_price`, `property_id`, dados do hóspede na reserva e `channel_connections`)
- Adapter compartilhado preserva o contrato do template PDF e elimina duplicação entre a prévia JSON e o download
- Consulta histórica executada pelo cliente administrativo somente depois da autenticação e limitada à organização e às propriedades autorizadas do usuário
- Perfil enviado por query string removido; permissões são derivadas exclusivamente da sessão autenticada
- Paginação explícita evita truncamento silencioso em relatórios com mais de 1.000 reservas; textos livres são escapados no HTML do relatório
- Intervalo usa checkout exclusivo, evitando contar como ocupação a reserva encerrada no primeiro dia do relatório
- Colunas "Ad." / "Cr." / "Notas" adicionadas à tabela HTML
- Layout: `table-layout: fixed` + classes de largura por coluna (`col-date`, `col-guest`, `col-num`, `col-notes`, `col-value`)
- PDF orientação alterada para landscape para acomodar colunas extra
- Gates: lint, typecheck e 201 suítes Jest (2.753 testes) aprovados; build de produção aprovado com webpack (o Turbopack local não pôde abrir a porta interna exigida pelo loader CSS)

### File List
- `src/app/api/reports/reservations-pdf/route.ts` (modified)
- `src/app/api/reports/reservations-pdf-download/route.ts` (modified)
- `src/app/[locale]/reservations/export/page.tsx` (modified)
- `src/components/features/reports/ReservationsPdfGenerator.tsx` (modified)
- `src/lib/reports/reservationReportData.ts` (created)
- `src/lib/reports/__tests__/reservationReportData.test.ts` (created)

### Change Log
- 2026-05-04: Story 16.4 completa (Dex, claude-sonnet-4-6)
- 2026-08-13: Exportação restaurada após migração Multi-OTA. As duas rotas agora compartilham consulta canônica, autorização por organização/propriedade e adapter testado para o template PDF (Dex).


---

## QA Results

### Review Date: 2026-05-06
### Reviewed By: Quinn (Test Architect)
### Code Quality Assessment
Ambas as routes actualizadas (JSON + download), notas truncadas a 40 chars, layout landscape com table-layout:fixed e col classes.
### Refactoring Performed
Nenhum. Zero correcções necessárias.
### Compliance Check
- All ACs Met: ✓ AC1–AC4
### Gate Status
Gate: PASS (100/100) → docs/qa/gates/16.4-reservation-pdf-export.yml
### Recommended Status
[✓ Ready for Done]
