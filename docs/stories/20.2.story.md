# Story 20.2: Stripe Session + Reserva — Currency Dinâmico

**Epic:** 20 — Checkout Directo (Housekeeping)
**Status:** Done
**Prioridade:** P0 — Bug: Stripe session e reserva hardcodam currency 'EUR'/'eur'
**Tipo:** Bugfix
**Agente:** @dev
**Depende de:** Story 20.1 ✅

---

## Context

`/api/public/bookings/route.ts` tinha dois hardcodes de currency:
1. `currency: 'EUR'` na inserção da reserva (MEDIUM concern identificado por Quinn em 20.1 QA)
2. `currency: 'eur'` na sessão Stripe (Stripe exige lowercase; DB armazena uppercase)

Para propriedades BRL/USD:
- Reserva ficava registada com currency: 'EUR' — wrong
- Stripe cobrava em EUR em vez da moeda da propriedade — wrong

---

## Acceptance Criteria

- [x] **AC1** — `currency` adicionado ao `.select()` da query de propriedade
- [x] **AC2** — Reserva inserida com `currency: property.currency ?? 'EUR'`
- [x] **AC3** — Stripe session usa `currency: (property.currency ?? 'EUR').toLowerCase()`
- [x] **AC4** — Build PASS + Lint PASS

---

## Tasks

- [x] 1. Fix currency em `/api/public/bookings/route.ts` (AC1–AC3)
- [x] 2. Build PASS + Lint PASS (AC4)

---

## File List

```
src/app/api/public/bookings/route.ts  ← modified
docs/stories/20.2.story.md            ← created (retroactivo)
```

## Dev Agent Record

### Status: Done
### Agent Model Used: claude-sonnet-4-6

### Completion Notes
- `.toLowerCase()` necessário: Stripe exige lowercase (`eur`, `brl`, `usd`), DB armazena uppercase
- Sem nova dependência

### Change Log
- 2026-05-07: Story 20.2 — Stripe + reservation currency fix (Dex, claude-sonnet-4-6)

---

## QA Results

### Review Date: 2026-05-07
### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Fix minimal e directo. Currency lida da propriedade e aplicada em dois pontos. `.toLowerCase()` correcto para Stripe API. Fallback `'EUR'` garante retrocompatibilidade. 0 TypeScript errors, 0 ESLint errors.

### Gate Status
Gate: PASS (100/100)

### Recommended Status
[✓ Done]
