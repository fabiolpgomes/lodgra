# Story 29.2 — Cleaner Access: Autenticação via WhatsApp Link

**Epic:** 29 — Cleaner Operations Portal  
**Status:** Draft  
**Prioridade:** P0 — Blocker para 29.3, 29.4, 29.5  
**Tipo:** Backend + Frontend (Auth)  
**Agente:** @dev  
**Depende de:** 29.1 (schema), 30.1 (WhatsApp API)  
**Complexity:** HIGH (5 story points)

---

## Story

**Como** gestor,  
**Quero** enviar um link de acesso via WhatsApp ao cleaner,  
**Para que** ele aceda ao portal sem precisar de password ou conta no sistema principal.

---

## Contexto de Negócio

Os cleaners não são utilizadores do sistema de gestão. Precisam de um acesso simples, mobile-first, sem fricção de registo. O fluxo é: gestor cria cleaner no sistema → sistema envia link WhatsApp → cleaner clica → acede ao seu painel.

### O que já existe
- RBAC com `guest_type` (staff, owner)
- Tabela `cleaner_access_tokens` (story 29.1)
- WhatsApp Cloud API (story 30.1)

### O que falta
- Novo `guest_type = 'cleaner'` em `user_profiles`
- Endpoint `/api/cleaners/send-access-link` — gera token e envia WhatsApp
- Endpoint `/api/cleaners/auth` — valida token, cria sessão
- Middleware de protecção da rota `/cleaner/`
- Formulário de cadastro de cleaner no painel do gestor

---

## Critérios de Aceitação

- [ ] **AC1** — Gestor pode criar cleaner em `/admin/users/new` com role=`guest`, guest_type=`cleaner`
- [ ] **AC2** — Botão "Enviar link de acesso" no perfil do cleaner gera token (TTL: 24h) e envia WhatsApp
- [ ] **AC3** — URL do link: `https://app.lodgra.io/cleaner/auth?token=XXXXX`
- [ ] **AC4** — Ao clicar no link, cleaner é autenticado e redirecionado para `/cleaner/dashboard`
- [ ] **AC5** — Token inválido/expirado mostra página de erro com opção de pedir novo link
- [ ] **AC6** — Sessão do cleaner dura 8h (renovável via WhatsApp)
- [ ] **AC7** — Cleaner só vê dados da sua própria organização (RLS)
- [ ] **AC8** — Gestor pode revogar acesso a qualquer momento
- [ ] **AC9** — Testes: geração de token, validação, expiração, revogação (6+ testes)

---

## Fluxo de Autenticação

```
Gestor → Cria cleaner → Clica "Enviar link"
  → POST /api/cleaners/send-access-link
  → Gera token (32 bytes hex, expires 24h)
  → Envia WhatsApp: "Olá [nome]! Acesse suas tarefas: [link]"
  
Cleaner → Clica link → GET /cleaner/auth?token=XXXXX
  → POST /api/cleaners/auth { token }
  → Valida token (existe, não expirado, não revogado)
  → Cria sessão JWT com cleaner_id + organization_id
  → Redirect → /cleaner/dashboard
```

---

## Riscos & Mitigação

**Token brute-force** — Tokens curtos podem ser adivinhados. **Mitigação:** 32 bytes hex = 256 bits de entropia; rate limit no endpoint de auth (5 tentativas/min por IP).

**Link partilhado** — Cleaner partilha link com terceiros. **Mitigação:** Sessão limitada a 8h; token de uso único configurável por organização.

---

## Tasks

- [ ] 1. Adicionar `guest_type = 'cleaner'` ao enum/check constraint
- [ ] 2. Criar `POST /api/cleaners/send-access-link` — gera token + envia WhatsApp
- [ ] 3. Criar `POST /api/cleaners/auth` — valida token, cria sessão
- [ ] 4. Criar middleware para rotas `/cleaner/*`
- [ ] 5. Criar página `/cleaner/auth/page.tsx` — validação do token
- [ ] 6. Criar página de erro `/cleaner/auth/error/page.tsx`
- [ ] 7. Actualizar UI de criação de utilizador para incluir `cleaner`
- [ ] 8. Escrever 6+ testes (token generation, validation, expiry, revocation)
- [ ] 9. Verificar linting e type checking

---

## Dev Notes

- JWT da sessão deve incluir: `{ sub: cleaner_id, org: organization_id, role: 'guest', guest_type: 'cleaner' }`
- Rate limit: usar `@upstash/ratelimit` ou middleware custom
- O envio WhatsApp delega para o serviço criado na Story 30.1

---

## Change Log

| Data | Agente | Alteração |
|------|--------|-----------|
| 2026-05-20 | @pm | Story criada |
