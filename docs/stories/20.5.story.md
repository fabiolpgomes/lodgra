# Story 20.5: PricingRulesManager — Currency Dinâmico na Tabela

**Epic:** 20 — Checkout Directo (Housekeeping)
**Status:** Done
**Prioridade:** P1 — Bug: tabela de regras de preço hardcoda `€` apesar de ter `currency` prop
**Tipo:** Bugfix
**Agente:** @dev

---

## Context

`PricingRulesManager.tsx` já tem prop `currency` e `getCurrencySymbol` importado, mas a tabela
de listagem de regras usa `€` hardcoded no header e em cada linha de preço.

---

## Acceptance Criteria

- [x] **AC1** — Header da tabela usa `getCurrencySymbol(currency)` em vez de `€`
- [x] **AC2** — Cada linha usa `getCurrencySymbol(currency)` em vez de `€`
- [x] **AC3** — Build PASS + Lint PASS

---

## Tasks

- [x] 1. Fix header + linha de preço em PricingRulesManager (AC1, AC2)
- [x] 2. Build PASS + Lint PASS (AC3)

---

## File List

```
src/components/features/pricing/PricingRulesManager.tsx  ← modified
docs/stories/20.5.story.md                               ← created
```

## Dev Agent Record

### Status: Done
### Agent Model Used: claude-sonnet-4-6

### Completion Notes
- Utilizou getCurrencySymbol já importado — zero novas dependências
- Padrão idêntico ao que estava em uso no form de edição (linha 234)

### Change Log
- 2026-05-07: Story 20.5 — dynamic currency in PricingRulesManager table (Dex, claude-sonnet-4-6)

---

## QA Results

### Review Date: 2026-05-07
### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Fix minimal e correcto. Usa getCurrencySymbol já importado no componente. Consistente com o form de edição (linha 234 já usava este padrão). Zero novas deps. 0 TypeScript errors, 0 ESLint errors.

### Gate Status
Gate: PASS (100/100)

### Recommended Status
[✓ Done]
