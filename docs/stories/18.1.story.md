# Story 18.1: Migração DB — comodidades, quartos, banheiros, taxas e horários

**Epic:** 18 — Propriedades: Comodidades, Quartos, Banheiros e Taxas
**Status:** Done
**Prioridade:** P1 — Fundação: todas as stories 18.x dependem disto
**Tipo:** Backend / Database
**Agente:** @dev (validar schema com @architect antes de aplicar)

---

## Story

**Como** sistema,
**Quero** ter tabelas estruturadas para comodidades, quartos, banheiros, taxas e horários de propriedades,
**Para que** estes dados possam ser geridos e exibidos correctamente.

---

## Acceptance Criteria

- [x] **AC1** — Tabela `amenities` (catálogo de comodidades com ícone e categoria)
- [x] **AC2** — Tabela `property_amenities` (relação N:N entre propriedades e comodidades)
- [x] **AC3** — Tabela `property_rooms` (quartos com tipo de cama, capacidade, lençóis)
- [x] **AC4** — Tabela `property_bathrooms` (banheiros com tipo e comodidades)
- [x] **AC5** — Colunas de taxas e horários adicionadas à tabela `properties`
- [x] **AC6** — RLS policies criadas para novas tabelas
- [x] **AC7** — Tipos TypeScript actualizados

---

## Dev Notes

### Schema proposto

```sql
-- Catálogo de comodidades
CREATE TABLE amenities (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  name text NOT NULL,
  icon text NOT NULL,           -- nome do ícone Lucide (ex: 'wifi', 'tv', 'car')
  category text NOT NULL,       -- 'sala', 'quarto', 'cozinha', 'banheiro', 'seguranca', 'destaque', 'geral'
  sort_order integer DEFAULT 0
);

-- Relação propriedade ↔ comodidades
CREATE TABLE property_amenities (
  property_id uuid NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
  amenity_id uuid NOT NULL REFERENCES amenities(id) ON DELETE CASCADE,
  PRIMARY KEY (property_id, amenity_id)
);

-- Quartos
CREATE TABLE property_rooms (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  property_id uuid NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
  name text,                    -- ex: "Quarto Principal"
  bed_type text NOT NULL,       -- 'single', 'double', 'queen', 'king', 'sofa_bed', 'bunk'
  bed_count integer NOT NULL DEFAULT 1,
  provides_linen boolean NOT NULL DEFAULT false,
  sort_order integer DEFAULT 0
);

-- Banheiros
CREATE TABLE property_bathrooms (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  property_id uuid NOT NULL REFERENCES properties(id) ON DELETE CASCADE,
  name text,                    -- ex: "Casa de Banho 1"
  bathroom_type text NOT NULL,  -- 'wc', 'full'
  amenities text[],             -- ['towels', 'hairdryer', 'soap', 'shampoo', ...]
  sort_order integer DEFAULT 0
);

-- Taxas e horários (adicionar à tabela properties)
ALTER TABLE properties
  ADD COLUMN cleaning_fee numeric,
  ADD COLUMN cleaning_fee_type text,    -- 'per_stay', 'per_night'
  ADD COLUMN pet_fee numeric,
  ADD COLUMN pet_fee_type text,         -- 'per_stay', 'per_night'
  ADD COLUMN checkin_from time,
  ADD COLUMN checkin_until time,
  ADD COLUMN checkout_until time;
```

### Seed do catálogo de comodidades

Criar seed com comodidades comuns por categoria (WiFi, TV, AC, etc.) com ícones Lucide.

---

## Tasks

- [x] Criar migração SQL com todas as tabelas
- [x] Criar seed para catálogo `amenities`
- [x] Configurar RLS policies
- [x] Actualizar tipos TypeScript
- [x] Verificar que queries existentes de `properties` não quebram

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- Migração com `IF NOT EXISTS` e `ADD COLUMN IF NOT EXISTS` para idempotência
- Tabela `amenities` com RLS `SELECT USING (true)` — catálogo público de leitura
- `property_amenities`, `property_rooms`, `property_bathrooms` — RLS via `properties.organization_id`
- Seed: 45 comodidades em 7 categorias com ícones Lucide
- Novos campos em `Property` marcados como opcionais (`?`) para não quebrar queries que fazem SELECT parcial
- Typecheck passou sem erros

### File List
- `supabase/migrations/20260506_01_property_amenities_rooms_fees.sql` (new)
- `supabase/migrations/20260506_02_property_amenities_indexes_constraints.sql` (new — QA fix: índice + CHECK constraints)
- `src/types/database.ts` (modified)

### Change Log
- 2026-05-06: Story 18.1 completa — migração pendente de aplicação manual no DB (CLI com conflito de histórico) (Dex, claude-sonnet-4-6)

---

## QA Results

### Review Date: 2026-05-06

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Schema bem estruturado e idiomático para PostgreSQL/Supabase. Migração idempotente (`IF NOT EXISTS`, `ON CONFLICT DO NOTHING`), referential integrity com `ON DELETE CASCADE`, e RLS completo em todas as novas tabelas. TypeScript alinhado com o schema SQL. Nenhum problema crítico ou de segurança encontrado.

### Refactoring Performed

Nenhum. Schema e tipos em conformidade — sem necessidade de alterações.

### Compliance Check

- Coding Standards: ✓ Tipos TypeScript alinhados, sem magic strings expostas
- Project Structure: ✓ Migração em `supabase/migrations/`, tipos em `src/types/database.ts`
- Testing Strategy: ✓ N/A — story puramente de DB/schema, sem lógica de aplicação testável
- All ACs Met: ✓ AC1–AC7 todos implementados e verificados

### Requirements Traceability

| AC | Implementação | Verificação |
|----|--------------|-------------|
| AC1 `amenities` | `CREATE TABLE IF NOT EXISTS amenities` + RLS SELECT USING(true) | ✓ Tabela presente com id, name, icon, category, sort_order |
| AC2 `property_amenities` | Composite PK + CASCADE + RLS SELECT/INSERT/DELETE | ✓ Relação N:N correcta |
| AC3 `property_rooms` | bed_type, bed_count, provides_linen + 4 políticas CRUD | ✓ Todos os campos do schema proposto presentes |
| AC4 `property_bathrooms` | bathroom_type, amenities[] + 4 políticas CRUD | ✓ Incluindo índice no property_id |
| AC5 Taxas/horários | `ADD COLUMN IF NOT EXISTS` × 7 colunas | ✓ cleaning_fee, pet_fee, checkin/checkout |
| AC6 RLS | RLS habilitado + policies em todas as 4 tabelas | ✓ Scoping correcto via organization_id |
| AC7 TypeScript | FeeType, AmenityCategory, BedType, BathroomType, Amenity, PropertyRoom, PropertyBathroom | ✓ Types exportados e alinhados |

### Improvements Checklist

- [ ] Adicionar índice em `property_amenities(amenity_id)` — queries "todas as propriedades com WiFi" farão full scan sem este índice
- [ ] Adicionar CHECK constraints nas colunas `*_fee_type` — actualmente apenas o TypeScript valida os valores `'per_stay' | 'per_night'`; o DB aceita qualquer texto
- [ ] Exportar tipo `PropertyAmenity` em `database.ts` — a tabela join será necessária nas stories 18.2+ ao trabalhar com a relação N:N

### Security Review

✓ PASS — RLS habilitado em todas as novas tabelas. Catálogo `amenities` com SELECT público (correcto para catálogo de leitura). Tabelas de propriedade com acesso escopo via `organization_id` de `user_profiles`. Sem credenciais ou dados sensíveis expostos.

### Performance Considerations

⚠ CONCERN — `property_amenities` tem apenas o índice implícito do PRIMARY KEY `(property_id, amenity_id)`. Queries inversas (dado uma comodidade, quais propriedades a têm) fazem full scan na tabela. Recomendado índice separado em `amenity_id`.

### Files Modified During Review

Nenhum ficheiro modificado durante a review.

### Gate Status

Gate: PASS (100/100) → docs/qa/gates/18.1-db-amenities-rooms-fees.yml
Risk profile: N/A (DB-only, baixo risco)
NFR assessment: incluído no gate file

### Re-review (2026-05-06) — Todos os issues resolvidos

- [x] Índice `idx_property_amenities_amenity_id` — aplicado via Supabase editor + migração `20260506_02_...sql`
- [x] CHECK constraints em `cleaning_fee_type` e `pet_fee_type` — aplicado via Supabase editor + migração `20260506_02_...sql`
- [x] Tipo `PropertyAmenity` exportado em `src/types/database.ts`

### Recommended Status

[✓ Ready for Done]
