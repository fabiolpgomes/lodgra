# Story 21.3: Checkout — Verificação de Disponibilidade na Página

**Epic:** 21 — Property Public Experience (Fase 2)
**Status:** Done
**Prioridade:** P1 — UX: hóspede não deve preencher formulário para depois descobrir datas indisponíveis
**Tipo:** Full Stack
**Agente:** @dev

---

## Story

**Como** hóspede que chega à página de checkout,
**Quero** ser avisado imediatamente se as datas já não estão disponíveis,
**Para que** não preencha o formulário desnecessariamente.

---

## Context

`checkout/page.tsx` valida mínimo de noites mas NÃO verifica disponibilidade. Se entre a
selecção de datas e o checkout outra reserva for confirmada, o hóspede só descobria o erro
ao submeter o POST — depois de preencher nome, email, telefone, etc.

---

## Acceptance Criteria

- [x] **AC1** — `checkout/page.tsx` verifica conflitos de reserva no load (two-step query)
- [x] **AC2** — Se conflito encontrado, redirect para `/p/[slug]?datesUnavailable=1`
- [x] **AC3** — Página pública mostra banner âmbar "As datas seleccionadas já não estão disponíveis"
- [x] **AC4** — Build PASS + Lint PASS

---

## Dev Notes

### Query de conflito (checkout/page.tsx)
```ts
const adminClient = createAdminClient()
const { data: listingsForCheck } = await adminClient
  .from('property_listings')
  .select('id')
  .eq('property_id', property.id)

const listingIdsForCheck = (listingsForCheck ?? []).map((l: { id: string }) => l.id)

if (listingIdsForCheck.length > 0) {
  const { data: conflicts } = await adminClient
    .from('reservations')
    .select('id')
    .in('property_listing_id', listingIdsForCheck)
    .in('status', ['confirmed', 'pending_payment'])
    .lt('check_in', checkout)
    .gt('check_out', checkin)
    .limit(1)

  if ((conflicts ?? []).length > 0) {
    redirect(`/p/${slug}?datesUnavailable=1`)
  }
}
```

---

## Tasks

- [x] 1. Add availability check in checkout/page.tsx (AC1, AC2)
- [x] 2. Add datesUnavailable prop + banner em PropertyPageV2 (AC3)
- [x] 3. Build PASS + Lint PASS (AC4)

---

## File List

```
src/app/p/[slug]/checkout/page.tsx              ← modified
src/app/p/[slug]/page.tsx                       ← modified
src/components/common/public/PropertyPageV2.tsx ← modified
docs/stories/21.3.story.md                      ← created
```

## Dev Agent Record

### Status: Done
### Agent Model Used: claude-sonnet-4-6

### Completion Notes
- Usa o mesmo padrão two-step que o `/api/public/bookings/route.ts` usa para double-booking check
- Inclui `pending_payment` para evitar race condition (alguém está no meio do Stripe checkout)
- Banner âmbar (não vermelho) — as datas não estão "erradas", apenas indisponíveis

### Change Log
- 2026-05-07: Story 21.3 — availability check em checkout/page.tsx (Dex, claude-sonnet-4-6)

---

## QA Results

### Review Date: 2026-05-07
### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Fix correcto. Two-step query idêntica ao padrão do booking API. Inclui `pending_payment` (correcto — alguém pode estar no Stripe). Guard para listingIdsForCheck vazio presente (array vazio skip). Redirect para URL semântica `?datesUnavailable=1`. Banner âmbar adequado (tom de aviso, não erro). PropertyPageV2 usa boolean prop — sem string parsing. 0 TypeScript errors, 0 ESLint errors.

### Compliance Check
- AC1–AC4: ✓ todos
- pending_payment incluído: ✓
- Guard para listingIds vazio: ✓

### Gate Status
Gate: PASS (100/100) → docs/qa/gates/21.3-checkout-availability-check.yml

### Recommended Status
[✓ Done]
