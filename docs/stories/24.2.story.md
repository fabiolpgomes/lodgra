# Story 24.2: Coordenadas Geográficas (lat/lng) nas Propriedades — Google VacationRental `geo`

**Epic:** 24 — Google Vacation Rentals Distribution
**Status:** Approved
**Prioridade:** P1 — Alta: elimina erro crítico do Google Rich Results Test
**Tipo:** Full Stack (DB migration + UI + SEO)
**Agente:** @dev

---

## Story

**Como** proprietário que lista imóveis no Lodgra,
**Quero** poder registar as coordenadas geográficas (latitude e longitude) de cada imóvel,
**Para que** o Google indexe correctamente o imóvel no Google Vacation Rentals com localização precisa.

---

## Context

O Google Rich Results Test para "Aluguel por temporada" reporta o erro crítico:
> **"O campo 'geo' não foi encontrado"**

O campo `geo` no Schema.org `VacationRental → containsPlace (Accommodation)` requer `GeoCoordinates` com `latitude` e `longitude`. A tabela `properties` na DB não tem estas colunas.

**Decisão de arquitectura — entrada manual (não geocoding automático):**
- Geocoding automático (Google Maps API, Nominatim) implica custo ou rate limits
- Para 7 propriedades activas, entrada manual é mais fiável e imediata
- O dashboard já tem o formulário de edição de propriedades em `src/app/[locale]/properties/[id]/edit/page.tsx`
- Adicionar dois campos `latitude` / `longitude` ao mesmo formulário é suficiente

**Dados de localização que já existem:**
- `address`, `city`, `country`, `postal_code` — já preenchidos e usados no schema
- `containsPlace → Accommodation` já está em produção (Story 24.1)

---

## Acceptance Criteria

- **AC1** — Migração Supabase adiciona `latitude DECIMAL(10,8)` e `longitude DECIMAL(11,8)` à tabela `properties`, ambas nullable
- **AC2** — O formulário de edição de propriedade (`/properties/[id]/edit`) tem dois campos numéricos: "Latitude" e "Longitude", com placeholder de exemplo (`38.7223`, `-9.1393`)
- **AC3** — O `UPDATE` ao guardar a propriedade inclui `latitude` e `longitude`
- **AC4** — O `generatePropertyJsonLd()` inclui `geo: { '@type': 'GeoCoordinates', latitude, longitude }` dentro de `containsPlace` quando ambos os valores existem
- **AC5** — A query da página pública `/p/[slug]` selecciona `latitude` e `longitude` e passa-os ao `generatePropertyJsonLd()`
- **AC6** — O Google Rich Results Test deixa de reportar "O campo 'geo' não foi encontrado" para propriedades com coordenadas preenchidas

---

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- **Primary Type**: Database + API + Frontend
- **Secondary Type(s)**: SEO / Integration (Google)
- **Complexity**: Low

**Specialized Agent Assignment:**
- **Primary**: @dev — implementação completa
- **Supporting**: @github-devops — push e deploy

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): `tsc --noEmit` + lint sem erros
- [ ] Pre-PR (@github-devops): push e deploy para validar no Google Rich Results Test

**CodeRabbit Focus Areas:**
- Precision correcta das colunas (`DECIMAL(10,8)` para lat, `DECIMAL(11,8)` para lng)
- Validação do input: latitude ∈ [-90, 90], longitude ∈ [-180, 180]
- Campos nullable — não quebrar propriedades sem coordenadas

---

## Tasks / Subtasks

- [ ] **Task 1** — Migração Supabase (AC1)
  - [ ] 1.1 — Criar ficheiro `supabase/migrations/20260511_01_property_geo_coordinates.sql`
  - [ ] 1.2 — `ALTER TABLE properties ADD COLUMN latitude DECIMAL(10,8);`
  - [ ] 1.3 — `ALTER TABLE properties ADD COLUMN longitude DECIMAL(11,8);`
  - [ ] 1.4 — Aplicar migração: `supabase db push` ou executar via Supabase Dashboard SQL Editor

- [ ] **Task 2** — Formulário de edição de propriedade (AC2 + AC3)
  - [ ] 2.1 — Em `src/app/[locale]/properties/[id]/edit/page.tsx`, adicionar os campos `latitude` e `longitude` ao formulário, na secção de localização (junto a `address`, `city`, etc.)
  - [ ] 2.2 — Adicionar os campos ao select inicial da propriedade (query de load)
  - [ ] 2.3 — Adicionar `latitude` e `longitude` ao objecto de `update` na submissão do formulário
  - [ ] 2.4 — Conversão de tipo: `parseFloat(formData.get('latitude'))` com fallback `null` se vazio

- [ ] **Task 3** — JSON-LD `geo` field (AC4 + AC5)
  - [ ] 3.1 — Adicionar `latitude?: number | null` e `longitude?: number | null` à interface `PropertyData` em `src/lib/seo/jsonld.ts`
  - [ ] 3.2 — Em `generatePropertyJsonLd()`, adicionar `geo` dentro de `containsPlace` quando `latitude` e `longitude` existem:
    ```typescript
    ...(property.latitude && property.longitude && {
      geo: {
        '@type': 'GeoCoordinates',
        latitude: property.latitude,
        longitude: property.longitude,
      }
    })
    ```
  - [ ] 3.3 — Em `src/app/p/[slug]/page.tsx`, adicionar `latitude, longitude` ao `.select(...)` da query da propriedade
  - [ ] 3.4 — Passar `latitude` e `longitude` ao `generatePropertyJsonLd()`

- [ ] **Task 4** — Validação (AC6)
  - [ ] 4.1 — Preencher coordenadas numa propriedade de teste via dashboard
  - [ ] 4.2 — Após deploy, testar no Google Rich Results Test — confirmar ausência do erro `geo`

---

## Dev Notes

### Ficheiros a modificar

| Ficheiro | Alteração |
|---|---|
| `supabase/migrations/20260511_01_property_geo_coordinates.sql` | Novo — ALTER TABLE |
| `src/app/[locale]/properties/[id]/edit/page.tsx` | Campos UI + select + update |
| `src/lib/seo/jsonld.ts` | Interface + `geo` em `containsPlace` |
| `src/app/p/[slug]/page.tsx` | Select `latitude, longitude` + passar ao jsonld |

### Padrão de formulário existente (replicar)

O formulário em `page.tsx` usa `formData.get('field_name')`. Exemplo do `postal_code` (linhas 173-178):
```typescript
.update({
  // ...
  postal_code: formData.get('postal_code') as string,
})
```

Para `latitude`/`longitude` usar `parseFloat` com fallback null:
```typescript
const lat = formData.get('latitude') ? parseFloat(formData.get('latitude') as string) : null
const lng = formData.get('longitude') ? parseFloat(formData.get('longitude') as string) : null
```

### Coordenadas de exemplo para teste
- Lisboa: `38.7223, -9.1393`
- Armação de Pêra: `37.1015, -8.3518`
- Porto: `41.1496, -8.6109`

Para obter coordenadas de qualquer morada: Google Maps → clique direito sobre o local → copia as coordenadas.

### Schema.org `geo` dentro de `containsPlace`

```json
"containsPlace": {
  "@type": "Accommodation",
  "address": { ... },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": 37.1015,
    "longitude": -8.3518
  },
  "numberOfRooms": 1
}
```

### Precision das colunas DB

- `DECIMAL(10,8)` para latitude: máx ±99.99999999 (cobre [-90, 90])
- `DECIMAL(11,8)` para longitude: máx ±999.99999999 (cobre [-180, 180])
- 8 casas decimais = precisão ~1mm (mais do que suficiente)

### Testing

- `tsc --noEmit` deve passar sem erros
- Verificar que propriedades SEM coordenadas continuam a funcionar (campos nullable)
- Verificar que propriedades COM coordenadas mostram `geo` no JSON-LD (inspecionar source da página pública)

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-11 | 1.0 | Story criada — geo coordinates para Google VacationRental | Gage (@devops) |

---

## Dev Agent Record

### Agent Model Used

_A preencher pelo agente de desenvolvimento_

### Debug Log References

_A preencher pelo agente de desenvolvimento_

### Completion Notes List

_A preencher pelo agente de desenvolvimento_

### File List

_A preencher pelo agente de desenvolvimento_
