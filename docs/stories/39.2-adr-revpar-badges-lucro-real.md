# Story 39.2: Badges MoM/YoY + Card ADR/RevPAR + Ajuste Lucro Real

**Epic:** 39 (Evolução do Dashboard — Analytics do Gestor)
**Story ID:** 39.2
**Status:** Ready (PO GO 10/10)
**Story Points:** 8
**Priority:** P1 — Grupo A (agente único, não fatiar)
**Depende de:** [39.1](39.1-schema-foundation-metrics.md) (Done)

---

## User Story

Como **gestor**, quero ver a variação mês a mês e ano a ano das principais métricas, um card combinado de ADR/RevPAR, e um Lucro Real que reflita corretamente a comissão de gestão menos as despesas da operação da empresa, para que eu entenda a tendência do negócio sem precisar comparar números de cabeça ou desconfiar de um lucro artificialmente alto quando ainda não lancei as despesas do mês.

---

## Por que é uma story só (Grupo A)

Badges MoM/YoY e o ajuste do Lucro Real tocam o **mesmo bloco de JSX** em `src/app/[locale]/dashboard/page.tsx` (linhas 585-640, o card "Lucro Real"). Se forem duas stories/agentes separados, um PR provavelmente sobrescreve o outro. Ver `docs/stories/epic-39-dashboard-analytics-evolution.md`, seção "Regra de despacho paralelo".

---

## Description

Três mudanças no dashboard atual (`src/app/[locale]/dashboard/page.tsx`), todas usando `monthly_property_metrics` (criada na 39.1) como fonte de dados históricos:

### A. Badges MoM/YoY nos 3 cards de KPI existentes + Receita do Mês

Cards afetados: "Propriedades/Reservas/Taxa de Ocupação" (linhas ~475-540), "Receita do Mês" (linhas ~542-583) e "Lucro Real" (linhas ~585-640). Adicionar um badge de variação percentual (mês anterior = MoM, mesmo mês ano anterior = YoY) ao lado do valor principal. Reaproveitar o padrão visual de badge já existente no próprio arquivo (linhas 618-624: `rounded px-1.5 py-0.5 text-[9px] font-bold`, cores por faixa) em vez de inventar um componente novo do zero.

**Fonte de dados:** somar `gross_revenue`/`nights_sold`/`booking_count` de `monthly_property_metrics` para o mês corrente vs. mês anterior (MoM) e mesmo mês do ano anterior (YoY), filtrado por `organization_id` e pelas propriedades do filtro ativo (`propertyIds`, já usado no restante da página).

### B. Card ADR/RevPAR combinado

Novo card, ao lado do card "Taxa de Ocupação" (decisão da spec-fonte, conflito 2: um único card combinado — ADR em destaque, RevPAR secundário, badge MoM). Fórmulas:
- `ADR = gross_revenue / nights_sold` (do mês corrente, de `monthly_property_metrics`)
- `RevPAR = ADR × (Taxa de Ocupação / 100)` — reaproveitar `currentMonthOccupancy`, já calculado na página.

Adicionar como 4º item no grid de KPIs (linhas ~475-540), ou logo abaixo dele em nova linha — decisão de layout do `@dev`/`@ux-design-expert`, desde que não quebre o grid em ≥1280px (critério de aceite da epic).

### C. Ajuste do Lucro Real (nível 4 do modelo de receita)

**Mudança de comportamento confirmada por Fabio:** o cálculo atual (linha 607: `const profit = revenue - expenses`, onde `expenses` vem de `monthExpensesByCurrency`, por sua vez de `supabase.from('expenses')` — despesas **por propriedade**, linhas 309-322) está errado para o que "Lucro Real" deveria significar. Trocar para:

```
Lucro Real = Σ calcManagementFee(gross_revenue_por_propriedade, management_percentage) − despesas da empresa no mês
```

**Não implementar do zero — reaproveitar o que já existe:**
- `calcManagementFee(grossRevenue, percentage)` — já existe em `src/lib/financial/calculations.ts:7`.
- `sumCompanyExpensesForYear(expenses, year).monthly[monthIndex]` — já existe em `src/lib/financial/company-expenses.ts:126`, já lida corretamente com `recurrence_type` (`none`/`monthly`/`yearly`) e exclui `status === 'cancelled'`. Ver uso de referência em `src/app/[locale]/dashboard/empresa/page.tsx` (linhas ~278-440), que já monta exatamente esse cálculo para o dashboard de sócios — **copiar o padrão de query/agregação de lá**, não reinventar.
- Query de `company_expenses` filtrada por `organization_id` e `expense_date` do ano corrente (a função já filtra por mês internamente via `getCompanyExpenseOccurrencesForYear`).

**Ajuste visual "sem despesas lançadas" (item 9 da spec, absorvido nesta story):** hoje, se `monthExpensesByCurrency` está vazio, `expenses = 0` e o card mostra margem 100% com badge verde — visualmente enganoso (parece que não há custo, quando na verdade é que ninguém lançou despesa ainda). Trocar a checagem: se não houver nenhuma linha de `company_expenses` com `status = 'paid'` para o mês corrente (nem recorrência ativa), mostrar um indicador diferente de "100% de margem" — por exemplo texto "sem despesas lançadas este mês" no lugar do badge de margem, ou o badge em tom neutro em vez de verde. Decisão visual exata fica com `@dev`, mas o critério de aceite é: **nunca comunicar 100% de margem como se fosse um resultado positivo quando o dado real é ausência de lançamento.**

---

## Acceptance Criteria

- [ ] Card ADR/RevPAR novo, combinado (ADR em destaque + RevPAR secundário + badge MoM), posicionado ao lado/próximo de "Taxa de Ocupação", sem quebrar o grid em ≥1280px.
- [ ] `RevPAR = ADR × Ocupação` — validado por teste automatizado (unitário, sobre a função de cálculo, não sobre o componente inteiro).
- [ ] Badges MoM/YoY adicionados a: Propriedades, Reservas, Taxa de Ocupação, Receita do Mês, Lucro Real, ADR/RevPAR.
- [ ] Badge mostra "—" (não "0%") quando não há período de comparação (ex.: propriedade nova sem histórico do mês anterior).
- [ ] Cor do badge segue o padrão verde/vermelho já usado no arquivo — **exceto Despesas**, que se for incluída em algum badge futuro (não é escopo desta story) seria invertida; aqui não se aplica pois Despesas fica na Story 39.7.
- [ ] Lucro Real passa a usar `calcManagementFee` (por propriedade, somado) menos despesas da empresa (`company_expenses`, via `sumCompanyExpensesForYear`), não mais `expenses` (tabela por propriedade).
- [ ] Card "sem despesas lançadas": quando não há `company_expenses` pagas/recorrentes ativas no mês corrente, o card não exibe badge de margem 100% em verde — usa indicador visual diferente (texto ou cor neutra) comunicando ausência de lançamento.
- [ ] Todos os valores monetários respeitam o toggle EUR/BRL já existente na página.
- [ ] Filtro de propriedade no topo (`propertyIds`) afeta ADR/RevPAR e os badges MoM/YoY (não afeta Lucro Real, que é sempre org-wide, pois comissão de gestão e despesas da empresa não são filtráveis por propriedade individual — confirmar esse entendimento com Fabio se parecer errado ao implementar).
- [ ] Reservas com `booking_source` ou `total_amount` ausentes (contadas em `incomplete_data_count` na view) não entram no cálculo de ADR/RevPAR nem são tratadas como zero.

---

## Definition of Done

- [ ] Todos os checkboxes de AC cumpridos.
- [ ] Teste unitário cobrindo `RevPAR = ADR × Ocupação` com pelo menos 3 casos (incluindo ocupação 0%).
- [ ] `npm run lint` e `npm run typecheck` limpos.
- [ ] Sem regressão nos testes existentes de `src/app/[locale]/dashboard/` e `src/lib/financial/`.
- [ ] Verificado visualmente (screenshot ou browser) que o grid não quebra em 1280px e 1920px.
- [ ] Lucro Real validado manualmente contra pelo menos 1 organização com dados reais (comparando com o que `/dashboard/empresa` já mostra para o mesmo mês — devem bater, já que ambos usam a mesma fonte agora).

---

## Scope

**IN:**
- Badges MoM/YoY nos cards existentes + Lucro Real + ADR/RevPAR
- Card ADR/RevPAR combinado (novo)
- Troca da fonte de dados do Lucro Real (`expenses` → `calcManagementFee` + `company_expenses`)
- Ajuste visual "sem despesas lançadas"

**OUT:**
- Qualquer outro card novo (Receita por Canal, Ranking, etc. — stories seguintes)
- Alterar a tabela `expenses` (despesas por propriedade) — ela continua existindo e sendo usada em outros lugares (`/financial`, `/reports/financeiro`), só não é mais a fonte do Lucro Real
- Prestação de contas ao proprietário (níveis 2/3 do modelo de receita) — fora de escopo da epic inteira

---

## Dependencies

- [39.1](39.1-schema-foundation-metrics.md) — `monthly_property_metrics` (Done)
- Reaproveita `src/lib/financial/calculations.ts` e `src/lib/financial/company-expenses.ts` (já existentes, não criados por esta story)

---

## File List (esperado — confirmar durante implementação)

**To Modify:**
- `src/app/[locale]/dashboard/page.tsx` (cards de KPI, Receita do Mês, Lucro Real, novo card ADR/RevPAR)

**To Create:**
- Função de cálculo de ADR/RevPAR + variação MoM/YoY (sugestão: `src/lib/dashboard/metrics.ts` ou similar — nome exato a critério do `@dev`, mas deve ser testável isoladamente do JSX)
- Componente de badge MoM/YoY reutilizável, se o padrão inline não escalar bem para 6 usos (avaliação do `@dev`)
- Testes unitários da função ADR/RevPAR

---

## Risks

| Risk | Mitigation |
|------|-----------|
| Lucro Real mudar de valor visível em produção pode confundir usuários que já viam o número antigo | Comunicar a mudança a Fabio antes do deploy (já sinalizado na Epic); considerar nota/tooltip no card explicando a mudança de metodologia na primeira semana |
| `monthly_property_metrics` não tem linha para meses sem nenhuma reserva (decisão da 39.1) | Badge deve mostrar "—", não quebrar nem mostrar `NaN%` |
| Duplicar lógica de `company_expenses` em vez de reaproveitar `sumCompanyExpensesForYear` | Ler `dashboard/empresa/page.tsx` antes de implementar — o padrão já está resolvido lá |

---

## Technical Notes

- `metric_month` em `monthly_property_metrics` é `DATE_TRUNC('month', check_in)` — atenção ao montar o filtro de "mês corrente" e "mês anterior" (usar primeiro dia do mês, não o dia atual).
- `calcManagementFee` espera `percentage` como número (ex.: 20 para 20%), igual a `properties.management_percentage` — não dividir por 100 antes de passar.
- `sumCompanyExpensesForYear` retorna `{ total, monthly }` onde `monthly` é um array de 12 `MoneyMap`s indexados 0-11 (janeiro=0) — usar `monthly[now.getMonth()]` para o mês corrente.

---

## Notes for Developer

1. Leia `src/app/[locale]/dashboard/empresa/page.tsx` inteiro antes de tocar no Lucro Real — a lógica de comissão + despesas da empresa já está resolvida e testada ali; o objetivo é reaproveitar, não reinventar.
2. Não delete nem modifique `ProfitCard.tsx`, `FinancialOverviewCharts.tsx`, `ProfitSummary.tsx`, `RevenueBreakdown.tsx` — são usados em `/financial` e `/reports/financeiro`, fora do escopo desta story.
3. Se o cálculo de MoM/YoY parecer que vai se repetir de forma idêntica em 3+ lugares, extrair para uma função compartilhada desde o início evita retrabalho nas Stories 39.3/39.4, que também vão precisar de comparação temporal.

---

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-07-23 | Claude Code (papel @sm) | Story criada — Draft |
| 2026-07-23 | Claude Code (papel @po) | Validação 10/10 — GO. Status Draft → Ready. |

---

## PO Validation

**Veredito:** GO (10/10) — título claro, descrição completa (com reaproveitamento concreto de `calcManagementFee`/`sumCompanyExpensesForYear` já existentes), AC testáveis, IN/OUT definido, dependência única e satisfeita (39.1 Done), estimativa de 8 pontos coerente com o volume (3 mudanças relacionadas no mesmo card), valor de negócio claro (Lucro Real deixa de mentir quando não há despesa lançada), 3 riscos documentados com mitigação, DoD claro, rastreável à spec-fonte e ao achado de Fabio sobre Algarve Home Stay.

**Observação do @po:** o AC sobre o filtro de propriedade não afetar o Lucro Real está marcado como "confirmar com Fabio se parecer errado" — isso é aceitável em Ready, não bloqueia; é uma suposição razoável documentada, não uma lacuna.

---

**Created by:** Claude Code (orquestração solicitada por Fabio, papel equivalente a `@sm`)
**Data:** 2026-07-23
