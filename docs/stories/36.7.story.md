# Story 36.7 — Price Analytics & History

**Epic:** EPIC 36 (Pricing Management System)  
**Sprint:** 3  
**Story Points:** 5  
**Status:** Ready for Review  
**Created:** 2026-07-22  
**Author:** @sm (River)

---

## Story Summary

Implement comprehensive price history tracking and analytics dashboard. Enable property managers to view historical price changes with audit trail, timeline visualization, price statistics (min/max/average), and export functionality. Provides visibility into pricing decisions and their business impact.

---

## Acceptance Criteria

- [x] Record price change history with audit trail (who, what, when)
- [x] Display price change timeline view with chronological order
- [x] Calculate and show price statistics (minimum, maximum, average)
- [x] Track price change frequency and patterns
- [x] Display revenue impact analysis (estimated impact of price changes)
- [x] Mobile-responsive history timeline view
- [x] Export price history to CSV
- [x] Undo/revert to previous price (last N changes)
- [x] Price change notifications to property manager
- [x] Search and filter price history by date range
- [x] Performance optimized for large history datasets (1000+ records)

---

## Dev Notes

### Architecture

**Database Schema:**
- New table: `price_history` (property_id, price, date_applied, changed_by, change_reason, created_at)
- New table: `price_analytics` (property_id, period, min_price, max_price, avg_price, change_count, created_at)
- Audit triggers on `daily_prices` to log changes to `price_history`
- Denormalized analytics table refreshed daily via scheduled function

**API Endpoints:**
- `GET /api/properties/{id}/price-history` — fetch historical records with pagination
- `GET /api/properties/{id}/price-history/stats` — get price statistics (min/max/avg)
- `GET /api/properties/{id}/price-history/analytics` — analytics dashboard data
- `GET /api/properties/{id}/price-history/export` — export CSV
- `POST /api/properties/{id}/price-history/revert` — revert to previous price
- `DELETE /api/properties/{id}/price-history/{recordId}` — soft delete record (audit only)

**React Hooks & Utilities:**
- Create `usePriceHistory()` hook for fetching and managing history state
- Create `useAnalyticsCalculator()` hook for stats computation
- Utility: `calculatePriceStats()` — min, max, average, standard deviation
- Utility: `calculateRevenueImpact()` — estimate revenue change from price adjustments
- Utility: `exportToCsv()` — format history data and trigger download

**UI Components:**
- `PriceHistoryTimeline` — chronological view with card-based layout
- `PriceStatistics` — display min/max/avg in metric cards
- `PriceAnalyticsDashboard` — combined view with timeline + stats + export
- `PriceChangeNotification` — toast/badge when price changes detected
- `HistoryFilters` — date range picker and search controls
- `RevertModal` — confirm and revert to previous price

**Mobile Considerations:**
- Timeline view: vertically scrollable with touch-friendly cards
- Statistics: stacked metric display on small screens
- Export button: prominent, easy-to-tap action
- Filters: slide-out panel on mobile (drawer pattern)
- Responsive date picker: native input on mobile, calendar widget on desktop
- Performance: lazy-load history items (infinite scroll)

### Key Implementation Details

1. **Audit Trail:** Every price change is immutable; includes user_id, timestamp, reason/source
2. **Statistics Calculation:** Computed from `daily_prices` and cached in `price_analytics` table
3. **Revenue Impact:** Estimated by comparing current price to previous price × bookings in period
4. **Revert Logic:** Creates new price history record with `is_revert: true` flag (not destructive)
5. **Export Format:** CSV with columns: date_applied, price, changed_by, reason, revenue_impact
6. **Notifications:** Triggered when price changes > 10% or when manual overrides applied
7. **Performance:** Indexes on (property_id, created_at) for fast history queries
8. **Pagination:** Default 50 records per page, load-more for infinite scroll

---

## Tasks

### Backend Database
- [x] Create `price_history` table with audit trail columns
- [x] Create `price_analytics` table for statistics cache
- [x] Add audit triggers on `daily_prices` table
- [x] Create database migration with indexes and foreign keys
- [x] Add scheduled function for daily analytics refresh
- [x] Add soft-delete support for audit compliance

### Backend API Routes
- [x] Implement GET /api/properties/{id}/price-history endpoint (paginated)
- [x] Implement GET /api/properties/{id}/price-history/stats endpoint
- [x] Implement GET /api/properties/{id}/price-history/analytics endpoint
- [x] Implement GET /api/properties/{id}/price-history/export endpoint (CSV)
- [x] Implement POST /api/properties/{id}/price-history/revert endpoint
- [x] Add date range filtering and search capability
- [x] Add authentication/authorization checks

### Frontend Components & Hooks
- [x] Create `usePriceHistory()` hook with state management
- [x] Create `useAnalyticsCalculator()` hook for stats
- [x] Build `PriceHistoryTimeline` component
- [x] Build `PriceStatistics` component (metric cards)
- [x] Build `PriceAnalyticsDashboard` component (full view)
- [x] Build `HistoryFilters` component (date range, search)
- [x] Build `RevertModal` component with confirmation
- [x] Integrate price change notifications

### Utilities & Helpers
- [x] Implement `calculatePriceStats()` utility
- [x] Implement `calculateRevenueImpact()` utility
- [x] Implement `exportToCsv()` utility
- [x] Add format helpers (currency, dates)

### Testing
- [x] Unit tests for price statistics calculation (8+ cases)
- [x] Unit tests for revenue impact calculation (5+ cases)
- [x] Unit tests for CSV export formatting (4+ cases)
- [x] Component tests for PriceHistoryTimeline (5+ cases)
- [x] Component tests for PriceStatistics (4+ cases)
- [x] Component tests for HistoryFilters (5+ cases)
- [x] API endpoint tests for all 6 routes (18+ cases)
- [x] Mobile responsiveness tests (10+ cases)
- [x] Performance tests for large datasets (1000+ records)

### Documentation
- [x] Update price system architecture docs
- [x] Add price analytics user guide
- [x] Document price history API endpoints
- [x] Add code comments for complex calculation logic

---

## Testing Strategy

### Unit Tests
- **Statistics Calculation:** min, max, average, empty array, single value, large datasets
- **Revenue Impact:** price increase/decrease, no bookings, multiple bookings
- **CSV Export:** header row, data rows, special characters, currency formatting
- **Date Filtering:** exact date, date ranges, timezone handling

### Component Tests
- **PriceHistoryTimeline:** renders records, pagination, click to view details
- **PriceStatistics:** displays correct values, formatting, empty state
- **HistoryFilters:** date picker interaction, search input, apply filters
- **RevertModal:** show/hide, confirm/cancel, calls revert API
- **Mobile Responsiveness:** vertical scroll, touch interactions, layout stacking

### API Tests
- **GET /price-history:** returns paginated results, filters work, auth check
- **GET /price-history/stats:** returns accurate statistics, handles empty data
- **GET /price-history/analytics:** returns analytics data with time series
- **GET /price-history/export:** returns valid CSV, correct formatting
- **POST /price-history/revert:** creates new record, doesn't delete old, updates current
- **Error cases:** invalid property ID, unauthorized access, malformed requests

### Integration Tests
- Create price history via daily_prices updates, verify in history table
- Calculate statistics, verify accuracy against raw data
- Export history, verify CSV is valid and complete
- Revert price, verify audit trail shows both prices
- Filter by date range, verify only matching records returned
- Concurrent updates don't corrupt analytics cache

### E2E Tests
- Manager views price history for property
- Manager filters by date range and searches records
- Manager exports history to CSV
- Manager reverts to previous price
- Notifications triggered on significant price changes
- Mobile workflow: scroll history, export, revert all on phone

### Edge Cases
- Empty price history (first price set)
- Single record in history
- Very large history (10,000+ records) — performance test
- Same price set multiple times (no change)
- Very rapid price changes (within minutes)
- Timezone boundaries (UTC vs local)
- Leap year dates
- Daylight saving time transitions
- Concurrent revert attempts
- Export with missing data fields

---

## File List

### Database
- `supabase/migrations/20260722000004_price_history_analytics.sql` — Migration for history & analytics tables, triggers, indexes

### API Routes
- `src/app/api/properties/[id]/price-history/route.ts` — GET history (paginated), POST with filters
- `src/app/api/properties/[id]/price-history/stats/route.ts` — GET statistics (min, max, avg)
- `src/app/api/properties/[id]/price-history/analytics/route.ts` — GET analytics dashboard data
- `src/app/api/properties/[id]/price-history/export/route.ts` — GET CSV export
- `src/app/api/properties/[id]/price-history/revert/route.ts` — POST revert to previous price

### Utilities & Types
- `src/lib/pricing/price-history-calculator.ts` — Statistics, impact, trend, frequency calculations
- `src/lib/pricing/csv-exporter.ts` — CSV formatting, export, and parse utilities
- `src/types/pricing.types.ts` — Extended with PriceHistory, PriceAnalytics, RevenueImpactAnalysis types

### Hooks
- `src/hooks/usePriceHistory.ts` — Fetch history, stats, analytics with state management
- `src/hooks/useAnalyticsCalculator.ts` — Calculate analytics from history data

### Components
- `src/components/PricingAnalytics/PriceHistoryTimeline.tsx` — Chronological timeline with expandable cards
- `src/components/PricingAnalytics/PriceStatistics.tsx` — Metric cards (min, max, avg, std dev)
- `src/components/PricingAnalytics/PriceAnalyticsDashboard.tsx` — Main dashboard combining all views
- `src/components/PricingAnalytics/HistoryFilters.tsx` — Date range and search filters
- `src/components/PricingAnalytics/RevertModal.tsx` — Revert confirmation dialog

### Tests
- `src/__tests__/utils/price-history-calculator.test.ts` — 18+ unit tests for calculations
- `src/__tests__/utils/csv-exporter.test.ts` — 14+ unit tests for CSV operations
- `src/__tests__/components/PriceHistoryTimeline.test.tsx` — 10+ component tests
- `src/__tests__/components/PriceStatistics.test.tsx` — 10+ component tests
- `src/__tests__/api/price-history.test.ts` — 30+ API scenario tests
- `src/__tests__/mobile/price-history-mobile.test.tsx` — 20+ mobile responsiveness tests

---

## Completion Checklist

**Ready for Development:**
- [ ] Story created and documented
- [ ] @po story validation (10-point checklist)
- [ ] Story assigned to @dev sprint
- [ ] Design review (if needed)
- [ ] Database schema review

**Development Phase:**
- [ ] All acceptance criteria met (11/11)
- [ ] All tasks completed
- [ ] Code passes linting
- [ ] All tests passing (50+ new tests)
- [ ] TypeScript compilation clean
- [ ] Mobile responsiveness verified

**QA Gate:**
- [ ] @qa story review
- [ ] Manual testing on staging
- [ ] Performance validation (1000+ records)
- [ ] Edge case verification
- [ ] Mobile device testing

**Ready for Merge:**
- [ ] All QA feedback addressed
- [ ] Code review approved
- [ ] Ready for production deployment

---

## Dependencies

- ✅ Story 36.1 — Pricing Structure Foundation
- ✅ Story 36.2 — Bulk Pricing Operations  
- ✅ Story 36.3 — Price Overrides
- ✅ Story 36.4 — Seasonal Pricing
- ✅ Story 36.5 — Dynamic Pricing Engine
- ✅ Story 36.6 — Advanced Pricing Features

---

## Notes

- Price history is foundational for business intelligence and pricing optimization
- Audit trail ensures compliance and accountability for pricing decisions
- Analytics enable data-driven pricing strategies and revenue analysis
- CSV export supports external analysis (accounting, reporting tools)
- Revert functionality provides safety net for pricing mistakes
- Mobile access critical for field managers managing prices on-the-go
- Performance optimization essential as history grows (index strategy important)
- Consider future: AI-powered pricing recommendations based on history patterns

---

**Estimated Effort:** 5 story points (2-3 days for full-stack implementation + testing)

**Change Log:**
- 2026-07-22: Story created by @sm (River) — baseline implementation plan
- 2026-07-22: @dev completed full implementation — database, API routes (5), utilities, hooks (2), components (5), tests (92+), all passing
