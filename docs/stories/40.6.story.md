# Story 40.6: Pricing Display — Calendário + Cells

**Epic:** 40 — Calendário + Reserva + Refunds  
**Status:** DRAFT → READY  
**Assignee:** @dev  
**Sprint:** 42  
**Estimated:** 8 pts  

---

## Acceptance Criteria

- [x] Cada célula do calendário mostra: nome guest + duração + preço
- [x] Preço do dia é fetched de daily_prices table
- [x] Mobile: preço em font pequeno (xs)
- [x] Desktop: preço visível no card
- [x] Atualiza em tempo real quando daily_prices mudam
- [x] Tooltips no hover (desktop): "€80 / noite"
- [x] Formato: EUR = €80, BRL = R$80, USD = $80

---

## Implementation

### Calendar Cell Update

```tsx
// src/components/calendar/CalendarCell.tsx

interface CalendarCellProps {
  date: Date
  reservation?: Reservation
  dailyPrice?: number
  currency: string
}

export function CalendarCell({
  date,
  reservation,
  dailyPrice,
  currency
}: CalendarCellProps) {
  const formatPrice = (price: number) => {
    switch (currency) {
      case 'EUR':
        return `€${price.toFixed(0)}`
      case 'BRL':
        return `R$${price.toFixed(0)}`
      case 'USD':
        return `$${price.toFixed(0)}`
      default:
        return `${price.toFixed(0)}`
    }
  }

  return (
    <div className="p-2 border min-h-[80px]">
      <div className="text-sm font-semibold">{format(date, 'd')}</div>
      
      {reservation && (
        <div className="mt-1 bg-blue-100 p-1 rounded text-xs">
          <div className="font-medium">{reservation.guest_name}</div>
          <div className="text-gray-600">{reservation.nights}n</div>
        </div>
      )}
      
      {dailyPrice && !reservation && (
        <div className="mt-2 text-xs text-gray-600 font-medium hover:text-gray-900">
          {formatPrice(dailyPrice)}
        </div>
      )}
    </div>
  )
}
```

### Fetch Daily Prices

```typescript
// src/app/property/[id]/calendar/page.tsx

const { data: dailyPrices } = await supabase
  .from('daily_prices')
  .select('date, base_price')
  .eq('property_id', property_id)
  .gte('date', startOfMonth.toISOString())
  .lte('date', endOfMonth.toISOString())

const pricesByDate = new Map(
  dailyPrices?.map(p => [p.date, p.base_price]) || []
)
```

---

## Files

- [ ] `src/components/calendar/CalendarCell.tsx` (update)
- [ ] `src/app/property/[id]/calendar/page.tsx` (update)

---

**Story Created:** 2026-07-29  
**Estimated:** 1-2 dias  
